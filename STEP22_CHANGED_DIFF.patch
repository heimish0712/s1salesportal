diff -ru /mnt/data/step22_base_flat/05_CustomerService.gs /mnt/data/step22_work/step21_work/05_CustomerService.gs
--- /mnt/data/step22_base_flat/05_CustomerService.gs	2026-06-28 08:49:03.193156250 +0000
+++ /mnt/data/step22_work/step21_work/05_CustomerService.gs	2026-06-28 08:45:50.868554380 +0000
@@ -162,18 +162,20 @@
 
 function getCustomerSearchIndexData(permForFilter) {
   const sheet = ensureCustomerSearchIndexSheet_();
+  const props = PropertiesService.getScriptProperties();
   let rows = getCustomerSearchIndexRows_();
 
-  // PATCH K-2: 기존 검색인덱스_DB가 detail-lite 확장 전 구조이면 자동으로 1회 재생성합니다.
-  // 단, 잠금 중이면 기존 rows를 반환하고 클라이언트가 다음 호출에서 다시 시도하게 합니다.
+  // STEP22/P310: 검색인덱스는 빠른 조회용 보조 캐시일 뿐이고, 원천은 항상 영업관리대장 마스터시트입니다.
+  // 마스터 직접수정/onEdit/변경큐 실패로 dirty 표시가 있으면, 다음 조회에서 먼저 마스터시트 기준 재생성을 시도합니다.
+  // 즉, dirty 상태의 오래된 검색인덱스를 웹앱 목록의 원천처럼 계속 노출하지 않습니다.
   let rebuildInfo = null;
   const needsK2Rebuild = !isCustomerSearchIndexK2Ready_();
+  const indexDirtyBefore = props.getProperty('CUSTOMER_SEARCH_INDEX_DIRTY') === 'Y';
 
-  // v31 FIX: 인덱스가 비어 있을 때 여러 웹앱 호출이 동시에 rebuild에 들어가며
-  // LockService waitLock 시간초과가 나는 문제를 막습니다.
-  // 첫 1명만 짧게 lock을 잡고 생성하고, 나머지는 빈 결과/생성중 상태를 반환합니다.
-  if (needsK2Rebuild || !rows.length) {
-    rebuildInfo = rebuildCustomerSearchIndex({ auto: true, maxWaitMs: 2500, reason: needsK2Rebuild ? 'K2_DETAIL_LITE' : 'EMPTY_INDEX' });
+  // v31 FIX 유지: 인덱스가 비어 있거나 구조가 맞지 않거나 dirty이면 첫 요청자가 짧게 rebuild합니다.
+  if (needsK2Rebuild || !rows.length || indexDirtyBefore) {
+    const reason = indexDirtyBefore ? 'DIRTY_MASTER_PRIORITY' : (needsK2Rebuild ? 'K2_DETAIL_LITE' : 'EMPTY_INDEX');
+    rebuildInfo = rebuildCustomerSearchIndex({ auto: true, maxWaitMs: 2500, reason: reason });
     if (rebuildInfo && rebuildInfo.ok) {
       rows = getCustomerSearchIndexRows_();
     }
@@ -183,11 +185,16 @@
   // 영업팀은 기본적으로 본인 영업담당자명과 매칭되는 고객만 내려줍니다.
   rows = filterPortalCustomerRowsByPermission_(rows, permForFilter);
 
-  const props = PropertiesService.getScriptProperties();
+  const dirtyAfter = props.getProperty('CUSTOMER_SEARCH_INDEX_DIRTY') === 'Y';
   return {
     ok: true,
     rebuilding: !!(rebuildInfo && rebuildInfo.locked),
     needsK2Rebuild: !!(needsK2Rebuild && !(rebuildInfo && rebuildInfo.ok)),
+    dirty: dirtyAfter,
+    dirtyReason: props.getProperty('CUSTOMER_SEARCH_INDEX_DIRTY_REASON') || '',
+    dirtyAt: props.getProperty('CUSTOMER_SEARCH_INDEX_DIRTY_AT') || '',
+    masterVersion: props.getProperty('PORTAL_MASTER_DATA_VERSION') || '',
+    masterChangedAt: props.getProperty('PORTAL_MASTER_DATA_CHANGED_AT') || '',
     message: rebuildInfo && rebuildInfo.locked ? rebuildInfo.message : '',
     version: props.getProperty('CUSTOMER_SEARCH_INDEX_VERSION') || getCustomerSearchIndexVersion_(),
     builtAt: props.getProperty('CUSTOMER_SEARCH_INDEX_BUILT_AT') || '',
@@ -196,6 +203,48 @@
   };
 }
 
+
+/**
+ * STEP22/P310: 서무/admin 전용 마스터시트 기준 검색인덱스 강제갱신.
+ * - 웹앱의 빠른 검색인덱스/브라우저 캐시가 영업관리대장 최신값보다 우선 보이는 상황을 복구합니다.
+ * - 권한은 서버에서 다시 검증합니다.
+ */
+function forceCustomerSearchIndexFromMasterP310(options) {
+  options = options || {};
+  const perm = getPortalCurrentPermission_();
+  if (!perm || perm.active === false || !perm.canUseAdminHome) {
+    throw new Error('마스터시트 기준 강제갱신은 서무/admin 계정에서만 사용할 수 있습니다.');
+  }
+  const startedAt = new Date();
+  const result = rebuildCustomerSearchIndex({
+    auto: false,
+    maxWaitMs: Math.max(3000, Math.min(15000, Number(options.maxWaitMs) || 10000)),
+    reason: 'ADMIN_FORCE_MASTER_PRIORITY'
+  });
+  if (!result || !result.ok) {
+    return Object.assign({ ok: false }, result || {}, {
+      message: result && result.message ? result.message : '검색인덱스 강제갱신에 실패했습니다.'
+    });
+  }
+
+  try { markPortalMasterDataChangedP201_('admin-force-search-index-from-master'); } catch (err) {}
+  try { clearCustomerSearchIndexDirty_(); } catch (err) {}
+  try { if (typeof clearContractCompleteCacheV69_ === 'function') clearContractCompleteCacheV69_(); } catch (err) {}
+  try { if (typeof clearFocusCustomerCacheP290 === 'function') clearFocusCustomerCacheP290(); } catch (err) {}
+
+  const fresh = getCustomerSearchIndexData(perm);
+  const elapsedMs = new Date().getTime() - startedAt.getTime();
+  return {
+    ok: true,
+    message: '영업관리대장 마스터시트 기준으로 검색인덱스_DB를 강제갱신했습니다.',
+    rebuilt: result.rebuilt || 0,
+    version: result.version || (fresh && fresh.version) || '',
+    builtAt: result.builtAt || (fresh && fresh.builtAt) || '',
+    elapsedMs: elapsedMs,
+    indexData: fresh
+  };
+}
+
 function getCustomerSearchIndexMeta() {
   const sheet = ensureCustomerSearchIndexSheet_();
   const props = PropertiesService.getScriptProperties();
diff -ru /mnt/data/step22_base_flat/Client_Router.html /mnt/data/step22_work/step21_work/Client_Router.html
--- /mnt/data/step22_base_flat/Client_Router.html	2026-06-28 08:49:03.197732175 +0000
+++ /mnt/data/step22_work/step21_work/Client_Router.html	2026-06-28 08:49:39.789684600 +0000
@@ -34,6 +34,90 @@
   if (btn) btn.innerHTML = document.body.classList.contains('sidebar-collapsed') ? '&gt;&gt;' : '&lt;&lt;';
 }
 
+
+
+function renderPortalSettingsPageP310_() {
+  const title = document.getElementById('placeholderTitle');
+  const desc = document.getElementById('placeholderDesc');
+  if (title) title.textContent = '설정';
+  const isAdmin = (typeof isPortalAdmin_ === 'function') && isPortalAdmin_();
+  if (!desc) return;
+  let html = '';
+  html += '<div class="portal-settings-panel-p310">';
+  html += '<div class="portal-settings-card-p310">';
+  html += '<div class="portal-settings-card-title-p310">마스터시트 기준 강제갱신</div>';
+  html += '<div class="portal-settings-card-desc-p310">영업관리대장 마스터시트를 원천으로 검색인덱스_DB와 브라우저 캐시를 다시 맞춥니다. 마스터시트에서 직접 수정한 진행현황/담당자/계약조건이 웹앱에 늦게 반영될 때 사용합니다.</div>';
+  if (isAdmin) {
+    html += '<button type="button" class="portal-danger-primary-p310" id="forceMasterSearchIndexBtnP310" onclick="forceMasterSearchIndexRefreshP310_()">마스터시트 기준 강제갱신</button>';
+    html += '<div class="portal-settings-status-p310" id="forceMasterSearchIndexStatusP310">대기 중</div>';
+  } else {
+    html += '<div class="portal-settings-denied-p310">이 기능은 서무/admin 계정에서만 사용할 수 있습니다.</div>';
+  }
+  html += '</div>';
+  html += '</div>';
+  desc.innerHTML = html;
+}
+
+function forceMasterSearchIndexRefreshP310_() {
+  if (typeof isPortalAdmin_ === 'function' && !isPortalAdmin_()) {
+    alert('마스터시트 기준 강제갱신은 서무/admin 계정에서만 사용할 수 있습니다.');
+    return;
+  }
+  const btn = document.getElementById('forceMasterSearchIndexBtnP310');
+  const status = document.getElementById('forceMasterSearchIndexStatusP310');
+  if (btn) btn.disabled = true;
+  if (status) status.textContent = '영업관리대장 마스터시트를 기준으로 검색인덱스를 다시 생성하는 중입니다...';
+  try { if (typeof invalidateCustomerSearchCaches_ === 'function') invalidateCustomerSearchCaches_({ clearIndex: true, clearDetail: true }); } catch (err) {}
+  try { customerSearchIndexStaleDueToMasterP310 = true; } catch (err) {}
+
+  google.script.run
+    .withSuccessHandler(function(res) {
+      if (btn) btn.disabled = false;
+      if (!res || !res.ok) {
+        const msg = (res && res.message) || '강제갱신에 실패했습니다.';
+        if (status) status.textContent = msg;
+        alert(msg);
+        return;
+      }
+      try { if (typeof invalidateCustomerSearchCaches_ === 'function') invalidateCustomerSearchCaches_({ clearIndex: true, clearDetail: true }); } catch (err) {}
+      try {
+        if (res.indexData && typeof setCustomerSearchIndexData_ === 'function') setCustomerSearchIndexData_(res.indexData, 'masterForce');
+        else if (typeof initializeCustomerSearchIndex === 'function') initializeCustomerSearchIndex({ silent: true, useLocalFirst: false, force: true, clearDetail: true, renderAfterLoad: true });
+      } catch (err) {}
+      try { customerSearchIndexStaleDueToMasterP310 = false; } catch (err) {}
+      try {
+        if (typeof portalLastDataVersionP201 !== 'undefined' && res.indexData) {
+          portalLastDataVersionP201.customerIndexVersion = res.indexData.version || portalLastDataVersionP201.customerIndexVersion;
+          portalLastDataVersionP201.masterVersion = res.indexData.masterVersion || portalLastDataVersionP201.masterVersion;
+          portalLastDataVersionP201.customerIndexDirty = res.indexData.dirty ? 'Y' : 'N';
+          portalLastDataVersionP201.customerIndexDirtyAt = res.indexData.dirtyAt || '';
+        }
+      } catch (err) {}
+      const message = '강제갱신 완료: ' + (res.rebuilt || 0) + '건 / 버전 ' + (res.version || '-') + ' / ' + Math.round((res.elapsedMs || 0) / 1000) + '초';
+      if (status) status.textContent = message;
+      try { toast(message); } catch (err) {}
+      try {
+        if (document.getElementById('customerSearchSection') && document.getElementById('customerSearchSection').classList.contains('active')) {
+          searchCustomerPage(customerSearchState && customerSearchState.page || 0);
+        }
+      } catch (err) {}
+      try {
+        if (typeof clearFocusCustomerClientCacheP300_ === 'function') clearFocusCustomerClientCacheP300_();
+        if (document.getElementById('focusCustomerSection') && document.getElementById('focusCustomerSection').classList.contains('active') && typeof loadFocusCustomerPageP290 === 'function') {
+          loadFocusCustomerPageP290(true);
+        }
+      } catch (err) {}
+      alert((res.message || '강제갱신 완료') + '\n\n반영 건수: ' + (res.rebuilt || 0) + '건\n버전: ' + (res.version || '-'));
+    })
+    .withFailureHandler(function(err) {
+      if (btn) btn.disabled = false;
+      const msg = '강제갱신 실패: ' + (err && err.message ? err.message : String(err || ''));
+      if (status) status.textContent = msg;
+      alert(msg);
+    })
+    .forceCustomerSearchIndexFromMasterP310({ maxWaitMs: 12000 });
+}
+
 function switchPage(page, skipSearch) {
   try { applyPortalPermissionToUi_(); } catch (err) {}
   page = page || 'home';
@@ -153,10 +237,17 @@
     return;
   }
 
+  if (page === 'settings') {
+    document.getElementById('placeholderSection').classList.add('active');
+    pageTitle.textContent = '설정';
+    pageSubtitle.textContent = '상태값, 권한, 캐시, 검색인덱스 관련 운영 기능을 관리합니다.';
+    renderPortalSettingsPageP310_();
+    setTimeout(function(){ try { applyPortalPermissionToUi_(); } catch (err) {} }, 50);
+    return;
+  }
+
   document.getElementById('placeholderSection').classList.add('active');
-  const labels = {
-    settings:['설정','상태값, 담당자, 수행사, 권한 설정을 관리하는 화면입니다.']
-  };
+  const labels = {};
   const item = labels[page] || ['준비 중','이 메뉴는 다음 단계에서 구현할 예정입니다.'];
   pageTitle.textContent = item[0];
   pageSubtitle.textContent = item[1];
diff -ru /mnt/data/step22_base_flat/Client_State.html /mnt/data/step22_work/step21_work/Client_State.html
--- /mnt/data/step22_base_flat/Client_State.html	2026-06-28 08:49:03.197992336 +0000
+++ /mnt/data/step22_work/step21_work/Client_State.html	2026-06-28 08:46:18.303695496 +0000
@@ -78,6 +78,8 @@
 let customerSearchIndexLoadSeqP230 = 0;
 let customerSearchIndexForceQueuedP230 = false;
 let customerSearchRequestSeqP230 = 0;
+// STEP22/P310: 마스터시트 변경이 감지되면 브라우저 검색인덱스/페이지캐시를 원천처럼 쓰지 않습니다.
+let customerSearchIndexStaleDueToMasterP310 = false;
 let customerDetailPrefetchTimers = [];
 let customerDetailPrefetching = false;
 const CUSTOMER_INDEX_STORAGE_KEY = 'salesPortal.customerSearchIndex.v36.detailLite';
@@ -549,7 +551,8 @@
       sessionStorage && Object.keys(sessionStorage).forEach(function(k) { if (String(k).indexOf('portalCustomerPage:') === 0) sessionStorage.removeItem(k); });
     }
   } catch (err) {}
-  try { initializeCustomerSearchIndex({ silent: true, useLocalFirst: false, force: true, clearDetail: true }); } catch (err) {}
+  try { customerSearchIndexStaleDueToMasterP310 = true; } catch (err) {}
+  try { initializeCustomerSearchIndex({ silent: true, useLocalFirst: false, force: true, clearDetail: true, renderAfterLoad: true }); } catch (err) {}
   try {
     if (document.getElementById('customerSearchSection') && document.getElementById('customerSearchSection').classList.contains('active')) {
       if (typeof searchCustomerPage === 'function') setTimeout(function(){ searchCustomerPage(customerSearchState && customerSearchState.page || 0); }, 250);
@@ -581,7 +584,8 @@
       sessionStorage && Object.keys(sessionStorage).forEach(function(k) { if (String(k).indexOf('portalCustomerPage:') === 0) sessionStorage.removeItem(k); });
     }
   } catch (err) {}
-  try { initializeCustomerSearchIndex({ silent: true, useLocalFirst: false, force: true, clearDetail: true }); } catch (err) {}
+  try { customerSearchIndexStaleDueToMasterP310 = true; } catch (err) {}
+  try { initializeCustomerSearchIndex({ silent: true, useLocalFirst: false, force: true, clearDetail: true, renderAfterLoad: true }); } catch (err) {}
   if (favoriteVisible) {
     try { loadCustomerFavoriteMap_({ silent: true, force: true }); } catch (err) {}
     try { if (typeof renderFavoritePage === 'function') renderFavoritePage(); } catch (err) {}
diff -ru /mnt/data/step22_base_flat/Component_CustomerSearch.html /mnt/data/step22_work/step21_work/Component_CustomerSearch.html
--- /mnt/data/step22_base_flat/Component_CustomerSearch.html	2026-06-28 08:49:03.200775498 +0000
+++ /mnt/data/step22_work/step21_work/Component_CustomerSearch.html	2026-06-28 08:48:35.439803911 +0000
@@ -164,6 +164,8 @@
 
 function canServeCustomerSearchFastP300_(keyword, page) {
   try {
+    if (typeof customerSearchIndexStaleDueToMasterP310 !== 'undefined' && customerSearchIndexStaleDueToMasterP310) return false;
+    if (customerSearchIndexState && customerSearchIndexState.dirty) return false;
     if (customerSearchIndexState && customerSearchIndexState.ready && Array.isArray(customerSearchIndexState.rows) && customerSearchIndexState.rows.length) return true;
   } catch (err) {}
   try {
@@ -190,11 +192,14 @@
   updateCustomerFilterSummary_();
   const canServeFast = canServeCustomerSearchFastP300_(keywordValue, pageNo);
   if (!canServeFast) {
-    resultArea.innerHTML = '<div class="loading">고객 데이터를 불러오는 중입니다...<br><span class="cache-note">브라우저 캐시 또는 검색인덱스_DB를 확인합니다.</span></div>';
+    resultArea.innerHTML = '<div class="loading">고객 데이터를 불러오는 중입니다...<br><span class="cache-note">영업관리대장/검색인덱스 최신 상태를 확인합니다.</span></div>';
+  }
+  const mustUseMasterPriorityP310ForSearch = !!(typeof customerSearchIndexStaleDueToMasterP310 !== 'undefined' && customerSearchIndexStaleDueToMasterP310) || !!(customerSearchIndexState && customerSearchIndexState.dirty);
+  if (!mustUseMasterPriorityP310ForSearch) {
+    initializeCustomerSearchIndex({ silent: true, useLocalFirst: true, skipNetworkIfReady: true, renderAfterLoad: false });
   }
-  initializeCustomerSearchIndex({ silent: true, useLocalFirst: true, skipNetworkIfReady: true, renderAfterLoad: false });
 
-  if (hasActiveCustomerFilterConditions_() && !(customerSearchIndexState && customerSearchIndexState.ready && Array.isArray(customerSearchIndexState.rows))) {
+  if (hasActiveCustomerFilterConditions_() && !mustUseMasterPriorityP310ForSearch && !(customerSearchIndexState && customerSearchIndexState.ready && Array.isArray(customerSearchIndexState.rows))) {
     waitCustomerFilterIndexThenSearch_(keywordValue, pageNo, requestSeq, requestSnapshot);
     return;
   }
@@ -2054,7 +2059,8 @@
   const loadedAtP300 = Number(customerSearchIndexState && customerSearchIndexState.loadedAt || 0);
   const ageP300 = loadedAtP300 ? (Date.now() - loadedAtP300) : Number.MAX_SAFE_INTEGER;
   const maxAgeP300 = Number(options.maxAgeMs || CUSTOMER_INDEX_BACKGROUND_REFRESH_MS_P300);
-  if (!force && readyRowsP300 && (options.skipNetworkIfReady || ageP300 < maxAgeP300)) {
+  const staleDueToMasterP310 = !!(typeof customerSearchIndexStaleDueToMasterP310 !== 'undefined' && customerSearchIndexStaleDueToMasterP310) || !!(customerSearchIndexState && customerSearchIndexState.dirty);
+  if (!force && !staleDueToMasterP310 && readyRowsP300 && (options.skipNetworkIfReady || ageP300 < maxAgeP300)) {
     return;
   }
 
@@ -2111,6 +2117,9 @@
 
 function setCustomerSearchIndexData_(data, source) {
   data = data || {};
+  if (data && data.dirty !== true) {
+    try { customerSearchIndexStaleDueToMasterP310 = false; } catch (err) {}
+  }
   // PATCH K-1: 서버/로컬 검색인덱스가 stale이어도, 현재 탭에서 마스터 full detail 또는
   // 사용자 저장으로 확정된 authoritative patch를 덮어쓰지 못하게 병합합니다.
   let rows = applyAuthoritativeCustomerPatchesToRowsK1_(Array.isArray(data.rows) ? data.rows : []);
@@ -2120,6 +2129,11 @@
     loading: false,
     version: data.version || String(Date.now()),
     builtAt: data.builtAt || '',
+    dirty: !!data.dirty,
+    dirtyReason: data.dirtyReason || '',
+    dirtyAt: data.dirtyAt || '',
+    masterVersion: data.masterVersion || '',
+    masterChangedAt: data.masterChangedAt || '',
     rows: rows,
     loadedAt: Date.now(),
     source: source || 'server'
@@ -2132,6 +2146,11 @@
   const payload = JSON.stringify({
     version: customerSearchIndexState.version,
     builtAt: customerSearchIndexState.builtAt,
+    dirty: !!customerSearchIndexState.dirty,
+    dirtyReason: customerSearchIndexState.dirtyReason || '',
+    dirtyAt: customerSearchIndexState.dirtyAt || '',
+    masterVersion: customerSearchIndexState.masterVersion || '',
+    masterChangedAt: customerSearchIndexState.masterChangedAt || '',
     loadedAt: customerSearchIndexState.loadedAt,
     permissionEmail: (getPortalPermission_() && getPortalPermission_().email) || '',
     cacheOwner: getPortalCustomerCacheOwnerP240_(),
@@ -2155,6 +2174,8 @@
     if (!raw) return false;
     const data = JSON.parse(raw);
     if (!data || !Array.isArray(data.rows) || !data.rows.length || !data.loadedAt) return false;
+    if (data.dirty) return false;
+    if (typeof customerSearchIndexStaleDueToMasterP310 !== 'undefined' && customerSearchIndexStaleDueToMasterP310) return false;
     if (Date.now() - Number(data.loadedAt) > CUSTOMER_INDEX_MAX_AGE_MS) return false;
     const currentPermEmail = (getPortalPermission_() && getPortalPermission_().email) || '';
     const currentOwner = getPortalCustomerCacheOwnerP240_();
@@ -2168,6 +2189,11 @@
       loading: false,
       version: data.version || 'local',
       builtAt: data.builtAt || '',
+      dirty: !!data.dirty,
+      dirtyReason: data.dirtyReason || '',
+      dirtyAt: data.dirtyAt || '',
+      masterVersion: data.masterVersion || '',
+      masterChangedAt: data.masterChangedAt || '',
       rows: filterCustomerRowsByPermissionClient_(applyAuthoritativeCustomerPatchesToRowsK1_(data.rows)),
       loadedAt: Number(data.loadedAt),
       source: 'localStorage'
@@ -3002,7 +3028,8 @@
 
 function fetchCustomerPage_(keyword, page, onSuccess, onError, options) {
   options = options || {};
-  if (!options.forceNetwork) {
+  const mustUseMasterPriorityP310 = !!(typeof customerSearchIndexStaleDueToMasterP310 !== 'undefined' && customerSearchIndexStaleDueToMasterP310) || !!(customerSearchIndexState && customerSearchIndexState.dirty);
+  if (!options.forceNetwork && !mustUseMasterPriorityP310) {
     const fromIndex = buildCustomerPageFromIndex_(keyword, page);
     if (fromIndex) {
       setCustomerPageCache_(keyword, page, fromIndex);
@@ -3015,7 +3042,7 @@
     }
   }
 
-  const cached = getCustomerPageFromCache_(keyword, page);
+  const cached = mustUseMasterPriorityP310 ? null : getCustomerPageFromCache_(keyword, page);
   if (cached && !options.forceNetwork) {
     onSuccess(cached, true);
     return;
diff -ru /mnt/data/step22_base_flat/Component_FocusCustomer.html /mnt/data/step22_work/step21_work/Component_FocusCustomer.html
--- /mnt/data/step22_base_flat/Component_FocusCustomer.html	2026-06-28 08:49:03.219238165 +0000
+++ /mnt/data/step22_work/step21_work/Component_FocusCustomer.html	2026-06-28 08:49:19.754064533 +0000
@@ -45,6 +45,16 @@
   focusCustomerStateP290_.sortMode = sortMode ? sortMode.value || 'priority' : 'priority';
 }
 
+
+function clearFocusCustomerClientCacheP300_() {
+  try {
+    if (focusCustomerStateP290_) {
+      focusCustomerStateP290_.cache = {};
+      focusCustomerStateP290_.data = null;
+    }
+  } catch (err) {}
+}
+
 function makeFocusCustomerClientCacheKeyP300_(payload) {
   try { return JSON.stringify(payload || {}); } catch (err) { return String(Date.now()); }
 }
Only in /mnt/data/step22_work/step21_work: STEP22_CHANGED_DIFF.patch
diff -ru /mnt/data/step22_base_flat/Styles.html /mnt/data/step22_work/step21_work/Styles.html
--- /mnt/data/step22_base_flat/Styles.html	2026-06-28 08:49:03.218564808 +0000
+++ /mnt/data/step22_work/step21_work/Styles.html	2026-06-28 08:47:46.242030375 +0000
@@ -4626,4 +4626,65 @@
   text-overflow: clip !important;
 }
 
+
+/* STEP22/P310: 설정 - 마스터시트 기준 검색인덱스 강제갱신 */
+.portal-settings-panel-p310 {
+  display: grid;
+  gap: 14px;
+  max-width: 860px;
+  margin: 0 auto;
+  text-align: left;
+}
+.portal-settings-card-p310 {
+  border: 1px solid #d7e3f5;
+  border-radius: 14px;
+  background: #fff;
+  padding: 18px 20px;
+  box-shadow: 0 8px 24px rgba(15, 23, 42, 0.06);
+}
+.portal-settings-card-title-p310 {
+  font-size: 16px;
+  font-weight: 900;
+  color: #0f172a;
+  margin-bottom: 8px;
+}
+.portal-settings-card-desc-p310 {
+  font-size: 13px;
+  line-height: 1.65;
+  color: #475467;
+  margin-bottom: 14px;
+}
+.portal-danger-primary-p310 {
+  min-height: 38px;
+  padding: 0 18px;
+  border: 0;
+  border-radius: 10px;
+  background: linear-gradient(135deg, #ff8a00, #ff5c35);
+  color: #fff;
+  font-weight: 900;
+  cursor: pointer;
+  box-shadow: 0 8px 18px rgba(255, 92, 53, 0.22);
+}
+.portal-danger-primary-p310:disabled {
+  opacity: 0.58;
+  cursor: wait;
+  box-shadow: none;
+}
+.portal-settings-status-p310 {
+  margin-top: 10px;
+  font-size: 12px;
+  font-weight: 800;
+  color: #344054;
+}
+.portal-settings-denied-p310 {
+  display: inline-block;
+  padding: 10px 12px;
+  border: 1px solid #fed7aa;
+  border-radius: 10px;
+  background: #fff7ed;
+  color: #9a3412;
+  font-size: 13px;
+  font-weight: 800;
+}
+
 </style>
