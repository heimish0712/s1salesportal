diff -ruN /mnt/data/step18_work/01_Schema.gs /mnt/data/step18_work_mod/01_Schema.gs
--- /mnt/data/step18_work/01_Schema.gs	2026-06-28 03:45:26.000000000 +0000
+++ /mnt/data/step18_work_mod/01_Schema.gs	2026-06-28 05:20:05.548910672 +0000
@@ -31,10 +31,10 @@
   area: { label: '연면적', headers: ['연면적', '연면적(㎡)', '연면적㎡', '면적'], type: 'numberText' },
   buildingType: { label: '건물 유형', headers: ['건물 유형', '건물유형'], type: 'select', options: ['', '기업', '공동주택', '학교', '공공', '기타'] },
   finalQuote: { label: '최종 견적가', headers: ['최종 견적가', '최종견적가', '최종단가', '최종 견적금액', '견적금액'], type: 'money' },
-  contractUnit: { label: '계약단위', headers: ['계약단위', '계약 단위'], type: 'select', options: ['', '6개월', '12개월'] },
+  contractUnit: { label: '계약단위', headers: ['계약단위', '계약 단위'], type: 'select', options: buildPortalContractUnitOptionsP280_() },
   appointment: { label: '관리자 선임 여부', headers: ['관리자\n선임 여부', '관리자 선임 여부', '관리자선임여부', '선임 여부', '관리자선임'], type: 'select', options: ['', '선임', '미선임', '해당없음'] },
-  maintenance: { label: '유지점검', headers: ['유지점검', '유지 점검'], type: 'select', options: ['', '0회', '1회', '2회', '3회', '4회', '5회', '6회'] },
-  performance: { label: '성능점검', headers: ['성능점검', '성능 점검'], type: 'select', options: ['', '0회', '1회', '2회', '3회', '4회', '5회', '6회'] },
+  maintenance: { label: '유지점검', headers: ['유지점검', '유지 점검'], type: 'select', options: buildPortalInspectionCountOptionsP280_(12) },
+  performance: { label: '성능점검', headers: ['성능점검', '성능 점검'], type: 'select', options: buildPortalInspectionCountOptionsP280_(12) },
   vat: { label: '부가세', headers: ['부가세', 'VAT', '부가세 여부'], type: 'select', options: ['', '별도', '포함'] },
   discountRate: { label: '할인율', headers: ['할인율(%)', '할인률(%)', '할인율', '할인률', '할인율 %'], type: 'numberText' },
   specialTerms: { label: '용역신청서특약사항', headers: ['용역신청서특약사항', '용역신청서 특약사항', '특약사항', '계약기간\n(상세 작성 요구한 경우만)'], type: 'textarea', wide: true },
@@ -50,6 +50,20 @@
   appointmentReportReceived: { label: '선임신고서', headers: ['선임신고서', '선임신고서 수취 여부', '선임신고서 수취여부', '선임신고서수취여부', '선임신고서 및 위임장 요청', '선임신고서 및 위임장', '선임신고서·위임장'], type: 'booleanMark' }
 };
 
+
+function buildPortalContractUnitOptionsP280_() {
+  const opts = [{ value: '', label: '-' }];
+  for (let i = 1; i <= 12; i++) opts.push({ value: String(i), label: i + '개월' });
+  return opts;
+}
+
+function buildPortalInspectionCountOptionsP280_(maxCount) {
+  maxCount = Number(maxCount) || 12;
+  const opts = [{ value: '', label: '-' }];
+  for (let i = 0; i <= maxCount; i++) opts.push({ value: String(i), label: i + '회' });
+  return opts;
+}
+
 function getMasterFieldSchema_() {
   return PORTAL_MASTER_FIELD_SCHEMA;
 }
diff -ruN /mnt/data/step18_work/05_CustomerService.gs /mnt/data/step18_work_mod/05_CustomerService.gs
--- /mnt/data/step18_work/05_CustomerService.gs	2026-06-28 03:45:26.000000000 +0000
+++ /mnt/data/step18_work_mod/05_CustomerService.gs	2026-06-28 05:22:17.241634022 +0000
@@ -1382,11 +1382,12 @@
     if (!Object.prototype.hasOwnProperty.call(values, def.key)) return;
     const col = findFirstExistingHeaderCol_(headerMap, def.headers || []) || ensureMasterColumn_(sheet, headerMap, (def.headers && def.headers[0]) || def.label);
     headerMap = getHeaderMap_(sheet);
-    const nextValue = String(values[def.key] == null ? '' : values[def.key]).trim();
+    const nextValue = getPortalMasterCompareTextP280_(def.key, values[def.key]);
+    const writeValue = getPortalMasterWriteValueP280_(def.key, values[def.key]);
     const range = sheet.getRange(rowNo, col);
     const prevValue = String(range.getDisplayValue() || '').trim();
     if (prevValue !== nextValue) {
-      range.setValue(nextValue);
+      range.setValue(writeValue);
       changed.push(def.label);
     }
   });
@@ -1455,8 +1456,9 @@
     if (!Object.prototype.hasOwnProperty.call(values, def.key)) return;
     const col = findFirstExistingHeaderCol_(headerMap, def.headers || []) || ensureMasterColumn_(sheet, headerMap, (def.headers && def.headers[0]) || def.label);
     headerMap = getHeaderMap_(sheet);
-    const nextValue = String(values[def.key] == null ? '' : values[def.key]).trim();
-    targets.push({ key: def.key, label: def.label, col: col, value: nextValue });
+    const nextValue = getPortalMasterCompareTextP280_(def.key, values[def.key]);
+    const writeValue = getPortalMasterWriteValueP280_(def.key, values[def.key]);
+    targets.push({ key: def.key, label: def.label, col: col, value: nextValue, writeValue: writeValue });
     appliedValues[def.key] = nextValue;
   });
 
@@ -1490,7 +1492,7 @@
   if (changedTargets.length) {
     changedTargets.sort(function(a, b) { return a.col - b.col; });
     let blockStart = changedTargets[0].col;
-    let blockValues = [changedTargets[0].value];
+    let blockValues = [changedTargets[0].writeValue];
     let prevCol = changedTargets[0].col;
 
     const flushBlock = function() {
@@ -1500,12 +1502,12 @@
     for (let i = 1; i < changedTargets.length; i++) {
       const t = changedTargets[i];
       if (t.col === prevCol + 1) {
-        blockValues.push(t.value);
+        blockValues.push(t.writeValue);
         prevCol = t.col;
       } else {
         flushBlock();
         blockStart = t.col;
-        blockValues = [t.value];
+        blockValues = [t.writeValue];
         prevCol = t.col;
       }
     }
@@ -1659,6 +1661,73 @@
 }
 
 
+
+// PATCH P280: 계약조건 숫자형 저장 호환
+// - 마스터시트 U/W/X(계약단위/유지점검/성능점검)는 숫자 셀을 기준으로 사용합니다.
+// - 화면에는 "12개월", "2회"처럼 보여도 저장 payload와 시트 write 값은 12, 2 같은 숫자로 정규화합니다.
+function normalizePortalContractUnitMonthsP280_(value) {
+  const text = String(value == null ? '' : value).trim();
+  if (!text || text === '-' || text === '–' || text === '—') return '';
+  if (/^1년$/.test(text)) return 12;
+  if (/^반년$/.test(text)) return 6;
+  const m = text.match(/\d+/);
+  if (!m) return '';
+  const n = Number(m[0]);
+  if (!isFinite(n) || n < 1 || n > 12) return '';
+  return n;
+}
+
+function normalizePortalInspectionCountP280_(value) {
+  const text = String(value == null ? '' : value).trim();
+  if (!text || text === '-' || text === '–' || text === '—') return '';
+  const m = text.match(/\d+/);
+  if (!m) return '';
+  const n = Number(m[0]);
+  if (!isFinite(n) || n < 0 || n > 12) return '';
+  return n;
+}
+
+function normalizePortalContractFieldForDbP280_(key, value) {
+  key = String(key || '').trim();
+  if (key === 'contractUnit') {
+    const months = normalizePortalContractUnitMonthsP280_(value);
+    return months === '' ? '' : String(months);
+  }
+  if (key === 'maintenance' || key === 'performance') {
+    const count = normalizePortalInspectionCountP280_(value);
+    return count === '' ? '' : String(count);
+  }
+  return value == null ? '' : String(value).trim();
+}
+
+function normalizePortalContractPayloadFieldsP280_(values) {
+  values = Object.assign({}, values || {});
+  ['contractUnit', 'maintenance', 'performance'].forEach(function(key) {
+    if (Object.prototype.hasOwnProperty.call(values, key)) {
+      values[key] = normalizePortalContractFieldForDbP280_(key, values[key]);
+    }
+  });
+  return values;
+}
+
+function getPortalMasterWriteValueP280_(key, value) {
+  key = String(key || '').trim();
+  if (key === 'contractUnit') {
+    const months = normalizePortalContractUnitMonthsP280_(value);
+    return months === '' ? '' : months;
+  }
+  if (key === 'maintenance' || key === 'performance') {
+    const count = normalizePortalInspectionCountP280_(value);
+    return count === '' ? '' : count;
+  }
+  return String(value == null ? '' : value).trim();
+}
+
+function getPortalMasterCompareTextP280_(key, value) {
+  const writeValue = getPortalMasterWriteValueP280_(key, value);
+  return String(writeValue == null ? '' : writeValue).trim();
+}
+
 // PATCH P1-12: 계약조건 저장 검증 + 할인율 공란 0 보정
 // - 변수 입력 부족이 시트 수식에 남지 않도록 계약 계산 필수값을 저장 전 차단합니다.
 // - 할인율은 공란이면 0으로 강제 저장합니다.
@@ -1690,7 +1759,7 @@
 
 function preparePortalContractValuesForSaveP112_(values, options) {
   options = options || {};
-  values = Object.assign({}, values || {});
+  values = normalizePortalContractPayloadFieldsP280_(Object.assign({}, values || {}));
   const requireFull = !!options.requireFull;
   const shouldValidate = requireFull || isPortalContractSaveTouchedP112_(values);
   if (!shouldValidate) return values;
@@ -1746,6 +1815,9 @@
   const maintenanceText = String(payload.maintenance || '').trim();
   const performanceText = String(payload.performance || '').trim();
   const vatText = String(payload.vat || '').trim();
+  const normalizedContractMonthsP280 = normalizePortalContractUnitMonthsP280_(contractUnitText);
+  const normalizedMaintenanceCountP280 = normalizePortalInspectionCountP280_(maintenanceText);
+  const normalizedPerformanceCountP280 = normalizePortalInspectionCountP280_(performanceText);
 
   const anyInput = [grade, discountTextRaw, contractUnitText, appointmentText, maintenanceText, performanceText, vatText, String(payload.targetFinalPrice || '').trim(), String(payload.area || '').trim()]
     .some(v => String(v || '').trim() !== '');
@@ -1756,10 +1828,10 @@
   const missing = [];
   if (!(area > 0)) missing.push('연면적');
   if (!grade) missing.push('관리등급');
-  if (isPortalContractValueBlankP112_(contractUnitText)) missing.push('계약단위');
+  if (isPortalContractValueBlankP112_(contractUnitText) || normalizedContractMonthsP280 === '') missing.push('계약단위');
   if (isPortalContractValueBlankP112_(appointmentText)) missing.push('관리자 선임 여부');
-  if (isPortalContractValueBlankP112_(maintenanceText)) missing.push('유지점검');
-  if (isPortalContractValueBlankP112_(performanceText)) missing.push('성능점검');
+  if (isPortalContractValueBlankP112_(maintenanceText) || normalizedMaintenanceCountP280 === '') missing.push('유지점검');
+  if (isPortalContractValueBlankP112_(performanceText) || normalizedPerformanceCountP280 === '') missing.push('성능점검');
   if (isPortalContractValueBlankP112_(vatText)) missing.push('부가세');
 
   if (missing.length) {
@@ -1780,10 +1852,10 @@
   const basis = getContractBasisForGrade_(grade);
   if (!basis) throw new Error('계약기준 시트에서 관리등급 [' + (grade || '-') + '] 기준단가를 찾지 못했습니다.');
 
-  const months = /12개월|1년/.test(contractUnitText) ? 12 : 6;
+  const months = normalizedContractMonthsP280 || 12;
   const hasAppointment = appointmentText === '선임' || (appointmentText.indexOf('선임') >= 0 && appointmentText.indexOf('미선임') < 0 && appointmentText.indexOf('해당없음') < 0);
-  const maintenanceCount = parseCount_(maintenanceText);
-  const performanceCount = parseCount_(performanceText);
+  const maintenanceCount = normalizedMaintenanceCountP280 === '' ? 0 : normalizedMaintenanceCountP280;
+  const performanceCount = normalizedPerformanceCountP280 === '' ? 0 : normalizedPerformanceCountP280;
 
   const appointmentAmount = hasAppointment ? basis.appointmentUnit * months : 0;
   const maintenanceAmount = basis.maintenanceUnit * maintenanceCount;
@@ -1813,7 +1885,7 @@
     ok: true,
     grade: grade,
     months: months,
-    contractUnit: months === 12 ? '12개월' : '6개월',
+    contractUnit: String(months),
     hasAppointment: hasAppointment,
     maintenanceCount: maintenanceCount,
     performanceCount: performanceCount,
@@ -1898,11 +1970,11 @@
   };
   setField('basic', 'firstRegisteredAt', todayText);
   setField('basic', 'status', '');
-  setField('contract', 'contractUnit', '12개월');
+  setField('contract', 'contractUnit', '12');
   setField('contract', 'vat', '별도');
   setField('contract', 'appointment', '선임');
-  setField('contract', 'maintenance', '2회');
-  setField('contract', 'performance', '1회');
+  setField('contract', 'maintenance', '2');
+  setField('contract', 'performance', '1');
 
   return detail;
 }
@@ -1987,7 +2059,7 @@
       col = ensureMasterColumn_(sheet, headerMap, headerName);
       headerMap = getHeaderMap_(sheet);
     }
-    sheet.getRange(rowNo, col).setValue(String(merged[def.key] == null ? '' : merged[def.key]).trim());
+    sheet.getRange(rowNo, col).setValue(getPortalMasterWriteValueP280_(def.key, merged[def.key]));
   });
 
   SpreadsheetApp.flush();
diff -ruN /mnt/data/step18_work/Component_CustomerDetail.html /mnt/data/step18_work_mod/Component_CustomerDetail.html
--- /mnt/data/step18_work/Component_CustomerDetail.html	2026-06-28 03:45:26.000000000 +0000
+++ /mnt/data/step18_work_mod/Component_CustomerDetail.html	2026-06-28 05:21:09.590232656 +0000
@@ -317,7 +317,7 @@
     if (f.key === 'status') {
       html += '<select class="detail-edit-input portal-status-select"' + data + ' disabled>' + renderStatusOptionsForDetail_(f.value || '') + '</select>';
     } else {
-      html += '<select class="detail-edit-input"' + data + ' disabled>' + renderGenericOptions(f.options || [], f.value || '') + '</select>';
+      html += '<select class="detail-edit-input"' + data + ' disabled>' + renderGenericOptions(f.options || [], f.value || '', f.key) + '</select>';
     }
   } else {
     const inputType = f.type === 'email' ? 'email' : 'text';
@@ -327,11 +327,46 @@
   return html;
 }
 
-function renderGenericOptions(options, current) {
-  options = options || []; current = current || '';
+function getGenericOptionValueP280_(opt) {
+  if (opt && typeof opt === 'object') return opt.value == null ? '' : String(opt.value);
+  return opt == null ? '' : String(opt);
+}
+
+function getGenericOptionLabelP280_(opt) {
+  if (opt && typeof opt === 'object') return opt.label == null ? getGenericOptionValueP280_(opt) : String(opt.label);
+  return opt == null || String(opt) === '' ? '-' : String(opt);
+}
+
+function normalizeDetailSelectCurrentP280_(key, value) {
+  const text = String(value == null ? '' : value).trim();
+  if (!text || text === '-' || text === '–' || text === '—') return '';
+  if (key === 'contractUnit') {
+    if (text === '1년') return '12';
+    if (text === '반년') return '6';
+    const m = text.match(/\d+/);
+    return m ? String(Math.max(1, Math.min(12, Number(m[0]) || 12))) : text;
+  }
+  if (key === 'maintenance' || key === 'performance') {
+    const m = text.match(/\d+/);
+    return m ? String(Math.max(0, Math.min(12, Number(m[0]) || 0))) : text;
+  }
+  return text;
+}
+
+function renderGenericOptions(options, current, key) {
+  options = (options || []).slice();
+  const normalizedCurrent = normalizeDetailSelectCurrentP280_(key || '', current);
   const seen = {}; let html = '';
-  if (current && options.indexOf(current) < 0) options.unshift(current);
-  options.forEach(opt => { if (seen[opt]) return; seen[opt]=true; html += '<option value="' + escAttr(opt) + '"' + (String(opt)===String(current)?' selected':'') + '>' + esc(opt || '-') + '</option>'; });
+  const hasCurrent = options.some(function(opt) { return String(getGenericOptionValueP280_(opt)) === String(normalizedCurrent); });
+  if (normalizedCurrent && !hasCurrent) options.unshift({ value: normalizedCurrent, label: current || normalizedCurrent });
+  options.forEach(function(opt) {
+    const value = getGenericOptionValueP280_(opt);
+    const label = getGenericOptionLabelP280_(opt);
+    const seenKey = String(value);
+    if (seen[seenKey]) return;
+    seen[seenKey] = true;
+    html += '<option value="' + escAttr(value) + '"' + (String(value) === String(normalizedCurrent) ? ' selected' : '') + '>' + esc(label || '-') + '</option>';
+  });
   return html;
 }
 
@@ -378,6 +413,36 @@
 };
 const DETAIL_CONTRACT_SAVE_KEYS_P112 = DETAIL_CONTRACT_REQUIRED_KEYS_P112.concat(['discountRate','grade','finalQuote','targetFinalPrice']);
 
+
+function normalizeDetailContractFieldForDbP280_(key, value) {
+  const text = String(value == null ? '' : value).trim();
+  if (key === 'contractUnit') {
+    if (!text || text === '-' || text === '–' || text === '—') return '';
+    if (text === '1년') return '12';
+    if (text === '반년') return '6';
+    const m = text.match(/\d+/);
+    if (!m) return text;
+    const n = Number(m[0]);
+    return n >= 1 && n <= 12 ? String(n) : '';
+  }
+  if (key === 'maintenance' || key === 'performance') {
+    if (!text || text === '-' || text === '–' || text === '—') return '';
+    const m = text.match(/\d+/);
+    if (!m) return text;
+    const n = Number(m[0]);
+    return n >= 0 && n <= 12 ? String(n) : '';
+  }
+  return text;
+}
+
+function normalizeDetailContractValuesForDbP280_(values) {
+  values = Object.assign({}, values || {});
+  ['contractUnit', 'maintenance', 'performance'].forEach(function(key) {
+    if (Object.prototype.hasOwnProperty.call(values, key)) values[key] = normalizeDetailContractFieldForDbP280_(key, values[key]);
+  });
+  return values;
+}
+
 function isDetailContractValueBlankP112_(value) {
   const text = String(value == null ? '' : value).trim();
   if (!text) return true;
@@ -393,12 +458,12 @@
 
 function prepareDetailContractValuesForClientSaveP112_(values, options) {
   options = options || {};
-  values = Object.assign({}, values || {});
+  values = normalizeDetailContractValuesForDbP280_(Object.assign({}, values || {}));
   const requireFull = !!options.requireFull;
   if (!requireFull && !isDetailContractSaveTouchedP112_(values)) return values;
 
   const currentValues = collectCurrentDetailInputValues_ ? collectCurrentDetailInputValues_() : gatherRegistrationValues();
-  const effective = Object.assign({}, currentValues || {}, values || {});
+  const effective = normalizeDetailContractValuesForDbP280_(Object.assign({}, currentValues || {}, values || {}));
   if (isDetailContractValueBlankP112_(effective.discountRate)) {
     effective.discountRate = '0';
     values.discountRate = '0';
@@ -642,7 +707,7 @@
     }
   }
 
-  setInputValueByKeyIfExists_('contractUnit', '12개월', { onlyBlank: true });
+  setInputValueByKeyIfExists_('contractUnit', '12', { onlyBlank: true });
   updateRegionVendorFromAddress_({ onlyIfAddress: true, onlyBlankVendor: true, normalizeAddress: false });
   enforceRegionReadonly_();
 }
@@ -759,9 +824,13 @@
 
 function normalizeContractUnitValue_(value) {
   const text = String(value || '').trim();
-  if (text === '12개월' || text === '1년') return '12개월';
-  if (text === '6개월' || text === '반년') return '6개월';
-  return text;
+  if (!text) return '';
+  if (text === '1년') return '12';
+  if (text === '반년') return '6';
+  const m = text.match(/\d+/);
+  if (!m) return text;
+  const n = Number(m[0]);
+  return n >= 1 && n <= 12 ? String(n) : text;
 }
 
 function applyContractUnitDefaults_(contractUnit, options) {
@@ -772,17 +841,17 @@
     return;
   }
 
-  if (unit === '12개월') {
+  if (unit === '12') {
     setInputValueByKeyIfExists_('appointment', '선임', options);
-    setInputValueByKeyIfExists_('maintenance', '2회', options);
-    setInputValueByKeyIfExists_('performance', '1회', options);
+    setInputValueByKeyIfExists_('maintenance', '2', options);
+    setInputValueByKeyIfExists_('performance', '1', options);
     setInputValueByKeyIfExists_('vat', '별도', options.onlyBlank ? { onlyBlank: true } : options);
     return;
   }
 
-  if (unit === '6개월') {
+  if (unit === '6') {
     setInputValueByKeyIfExists_('appointment', '선임', options);
-    setInputValueByKeyIfExists_('maintenance', '1회', options);
+    setInputValueByKeyIfExists_('maintenance', '1', options);
     // 6개월은 성능점검 0회/1회가 모두 쓰이므로 미리 선택하지 않습니다.
     setInputValueByKeyIfExists_('performance', '', options);
     setInputValueByKeyIfExists_('vat', '별도', options.onlyBlank ? { onlyBlank: true } : options);
@@ -794,7 +863,7 @@
   setInputValueByKeyIfExists_('vat', '별도', { onlyBlank: true });
   const unitEl = getDetailInputByKey_('contractUnit');
   if (currentDetail && currentDetail.isNew && unitEl && !String(unitEl.value || '').trim()) {
-    unitEl.value = '12개월';
+    unitEl.value = '12';
   }
   if (unitEl && String(unitEl.value || '').trim()) {
     applyContractUnitDefaults_(unitEl.value, { onlyBlank: true });
diff -ruN /mnt/data/step18_work/Component_CustomerSearch.html /mnt/data/step18_work_mod/Component_CustomerSearch.html
--- /mnt/data/step18_work/Component_CustomerSearch.html	2026-06-28 03:45:26.000000000 +0000
+++ /mnt/data/step18_work_mod/Component_CustomerSearch.html	2026-06-28 05:21:58.234712953 +0000
@@ -1453,6 +1453,14 @@
   if (key === 'serviceApplicationReceived' || key === 'appointmentReportReceived') {
     return normalizeCustomerExpandBooleanMark_(normalized);
   }
+  if (key === 'contractUnit') {
+    const v = normalizePortalContractDisplayValueClientP280_('contractUnit', normalized);
+    return v ? v + '개월' : disp(normalized);
+  }
+  if (key === 'maintenance' || key === 'performance') {
+    const v = normalizePortalContractDisplayValueClientP280_(key, normalized);
+    return v !== '' ? v + '회' : disp(normalized);
+  }
   if (key === 'phone' || key === 'directPhone') {
     return splitCustomerExpandPhoneLines_(normalized) || disp(normalized);
   }
@@ -3006,6 +3014,42 @@
 }
 
 
+
+function buildPortalContractUnitOptionsClientP280_() {
+  const opts = [{ value: '', label: '-' }];
+  for (let i = 1; i <= 12; i++) opts.push({ value: String(i), label: i + '개월' });
+  return opts;
+}
+
+function buildPortalInspectionCountOptionsClientP280_(maxCount) {
+  maxCount = Number(maxCount) || 12;
+  const opts = [{ value: '', label: '-' }];
+  for (let i = 0; i <= maxCount; i++) opts.push({ value: String(i), label: i + '회' });
+  return opts;
+}
+
+function normalizePortalContractDisplayValueClientP280_(key, value) {
+  const text = String(value == null ? '' : value).trim();
+  if (!text || text === '-' || text === '–' || text === '—') return '';
+  if (key === 'contractUnit') {
+    if (text === '1년') return '12';
+    if (text === '반년') return '6';
+    const m = text.match(/\d+/);
+    return m ? String(Math.max(1, Math.min(12, Number(m[0]) || 12))) : text;
+  }
+  if (key === 'maintenance' || key === 'performance') {
+    const m = text.match(/\d+/);
+    return m ? String(Math.max(0, Math.min(12, Number(m[0]) || 0))) : text;
+  }
+  return text;
+}
+
+function getPortalContractFieldOptionsClientP280_(key) {
+  if (key === 'contractUnit') return buildPortalContractUnitOptionsClientP280_();
+  if (key === 'maintenance' || key === 'performance') return buildPortalInspectionCountOptionsClientP280_(12);
+  return null;
+}
+
 function findCustomerRowSummaryByCustomerNo_(customerNo) {
   const cno = normalizeCustomerNoForKey_(customerNo);
   if (!cno) return null;
@@ -3056,10 +3100,10 @@
     grade: row.grade || '',
     buildingType: row.buildingType || '',
     finalQuote: row.finalQuote || '',
-    contractUnit: row.contractUnit || '',
+    contractUnit: normalizePortalContractDisplayValueClientP280_('contractUnit', row.contractUnit || ''),
     appointment: row.appointment || '',
-    maintenance: row.maintenance || '',
-    performance: row.performance || '',
+    maintenance: normalizePortalContractDisplayValueClientP280_('maintenance', row.maintenance || ''),
+    performance: normalizePortalContractDisplayValueClientP280_('performance', row.performance || ''),
     vat: row.vat || '',
     discountRate: row.discountRate || '',
     specialTerms: row.specialTerms || '',
@@ -3099,8 +3143,12 @@
     ['discountRate','할인율',true,'numberText'], ['specialTerms','용역신청서특약사항',true,'textarea',true]
   ];
   const make = function(def) {
-    const field = { key:def[0], label:def[1], editable:def[2], type:def[3], wide:!!def[4], value: detail[def[0]] || '' };
+    const key = def[0];
+    const opts = getPortalContractFieldOptionsClientP280_(key);
+    const value = opts ? normalizePortalContractDisplayValueClientP280_(key, detail[key] || '') : (detail[key] || '');
+    const field = { key:key, label:def[1], editable:def[2], type:def[3], wide:!!def[4], value: value };
     if (field.key === 'customerRank') field.options = ['', 'A급', 'B급', 'C급', '보류', '제외'];
+    if (opts) field.options = opts;
     return field;
   };
   return { basic: basicDefs.map(make), contract: contractDefs.map(make) };
diff -ruN /mnt/data/step18_work/STEP18_CHANGED_DIFF.patch /mnt/data/step18_work_mod/STEP18_CHANGED_DIFF.patch
--- /mnt/data/step18_work/STEP18_CHANGED_DIFF.patch	1970-01-01 00:00:00.000000000 +0000
+++ /mnt/data/step18_work_mod/STEP18_CHANGED_DIFF.patch	2026-06-28 05:23:46.060442346 +0000
@@ -0,0 +1,508 @@
+diff -ruN /mnt/data/step18_work/01_Schema.gs /mnt/data/step18_work_mod/01_Schema.gs
+--- /mnt/data/step18_work/01_Schema.gs	2026-06-28 03:45:26.000000000 +0000
++++ /mnt/data/step18_work_mod/01_Schema.gs	2026-06-28 05:20:05.548910672 +0000
+@@ -31,10 +31,10 @@
+   area: { label: '연면적', headers: ['연면적', '연면적(㎡)', '연면적㎡', '면적'], type: 'numberText' },
+   buildingType: { label: '건물 유형', headers: ['건물 유형', '건물유형'], type: 'select', options: ['', '기업', '공동주택', '학교', '공공', '기타'] },
+   finalQuote: { label: '최종 견적가', headers: ['최종 견적가', '최종견적가', '최종단가', '최종 견적금액', '견적금액'], type: 'money' },
+-  contractUnit: { label: '계약단위', headers: ['계약단위', '계약 단위'], type: 'select', options: ['', '6개월', '12개월'] },
++  contractUnit: { label: '계약단위', headers: ['계약단위', '계약 단위'], type: 'select', options: buildPortalContractUnitOptionsP280_() },
+   appointment: { label: '관리자 선임 여부', headers: ['관리자\n선임 여부', '관리자 선임 여부', '관리자선임여부', '선임 여부', '관리자선임'], type: 'select', options: ['', '선임', '미선임', '해당없음'] },
+-  maintenance: { label: '유지점검', headers: ['유지점검', '유지 점검'], type: 'select', options: ['', '0회', '1회', '2회', '3회', '4회', '5회', '6회'] },
+-  performance: { label: '성능점검', headers: ['성능점검', '성능 점검'], type: 'select', options: ['', '0회', '1회', '2회', '3회', '4회', '5회', '6회'] },
++  maintenance: { label: '유지점검', headers: ['유지점검', '유지 점검'], type: 'select', options: buildPortalInspectionCountOptionsP280_(12) },
++  performance: { label: '성능점검', headers: ['성능점검', '성능 점검'], type: 'select', options: buildPortalInspectionCountOptionsP280_(12) },
+   vat: { label: '부가세', headers: ['부가세', 'VAT', '부가세 여부'], type: 'select', options: ['', '별도', '포함'] },
+   discountRate: { label: '할인율', headers: ['할인율(%)', '할인률(%)', '할인율', '할인률', '할인율 %'], type: 'numberText' },
+   specialTerms: { label: '용역신청서특약사항', headers: ['용역신청서특약사항', '용역신청서 특약사항', '특약사항', '계약기간\n(상세 작성 요구한 경우만)'], type: 'textarea', wide: true },
+@@ -50,6 +50,20 @@
+   appointmentReportReceived: { label: '선임신고서', headers: ['선임신고서', '선임신고서 수취 여부', '선임신고서 수취여부', '선임신고서수취여부', '선임신고서 및 위임장 요청', '선임신고서 및 위임장', '선임신고서·위임장'], type: 'booleanMark' }
+ };
+ 
++
++function buildPortalContractUnitOptionsP280_() {
++  const opts = [{ value: '', label: '-' }];
++  for (let i = 1; i <= 12; i++) opts.push({ value: String(i), label: i + '개월' });
++  return opts;
++}
++
++function buildPortalInspectionCountOptionsP280_(maxCount) {
++  maxCount = Number(maxCount) || 12;
++  const opts = [{ value: '', label: '-' }];
++  for (let i = 0; i <= maxCount; i++) opts.push({ value: String(i), label: i + '회' });
++  return opts;
++}
++
+ function getMasterFieldSchema_() {
+   return PORTAL_MASTER_FIELD_SCHEMA;
+ }
+diff -ruN /mnt/data/step18_work/05_CustomerService.gs /mnt/data/step18_work_mod/05_CustomerService.gs
+--- /mnt/data/step18_work/05_CustomerService.gs	2026-06-28 03:45:26.000000000 +0000
++++ /mnt/data/step18_work_mod/05_CustomerService.gs	2026-06-28 05:22:17.241634022 +0000
+@@ -1382,11 +1382,12 @@
+     if (!Object.prototype.hasOwnProperty.call(values, def.key)) return;
+     const col = findFirstExistingHeaderCol_(headerMap, def.headers || []) || ensureMasterColumn_(sheet, headerMap, (def.headers && def.headers[0]) || def.label);
+     headerMap = getHeaderMap_(sheet);
+-    const nextValue = String(values[def.key] == null ? '' : values[def.key]).trim();
++    const nextValue = getPortalMasterCompareTextP280_(def.key, values[def.key]);
++    const writeValue = getPortalMasterWriteValueP280_(def.key, values[def.key]);
+     const range = sheet.getRange(rowNo, col);
+     const prevValue = String(range.getDisplayValue() || '').trim();
+     if (prevValue !== nextValue) {
+-      range.setValue(nextValue);
++      range.setValue(writeValue);
+       changed.push(def.label);
+     }
+   });
+@@ -1455,8 +1456,9 @@
+     if (!Object.prototype.hasOwnProperty.call(values, def.key)) return;
+     const col = findFirstExistingHeaderCol_(headerMap, def.headers || []) || ensureMasterColumn_(sheet, headerMap, (def.headers && def.headers[0]) || def.label);
+     headerMap = getHeaderMap_(sheet);
+-    const nextValue = String(values[def.key] == null ? '' : values[def.key]).trim();
+-    targets.push({ key: def.key, label: def.label, col: col, value: nextValue });
++    const nextValue = getPortalMasterCompareTextP280_(def.key, values[def.key]);
++    const writeValue = getPortalMasterWriteValueP280_(def.key, values[def.key]);
++    targets.push({ key: def.key, label: def.label, col: col, value: nextValue, writeValue: writeValue });
+     appliedValues[def.key] = nextValue;
+   });
+ 
+@@ -1490,7 +1492,7 @@
+   if (changedTargets.length) {
+     changedTargets.sort(function(a, b) { return a.col - b.col; });
+     let blockStart = changedTargets[0].col;
+-    let blockValues = [changedTargets[0].value];
++    let blockValues = [changedTargets[0].writeValue];
+     let prevCol = changedTargets[0].col;
+ 
+     const flushBlock = function() {
+@@ -1500,12 +1502,12 @@
+     for (let i = 1; i < changedTargets.length; i++) {
+       const t = changedTargets[i];
+       if (t.col === prevCol + 1) {
+-        blockValues.push(t.value);
++        blockValues.push(t.writeValue);
+         prevCol = t.col;
+       } else {
+         flushBlock();
+         blockStart = t.col;
+-        blockValues = [t.value];
++        blockValues = [t.writeValue];
+         prevCol = t.col;
+       }
+     }
+@@ -1659,6 +1661,73 @@
+ }
+ 
+ 
++
++// PATCH P280: 계약조건 숫자형 저장 호환
++// - 마스터시트 U/W/X(계약단위/유지점검/성능점검)는 숫자 셀을 기준으로 사용합니다.
++// - 화면에는 "12개월", "2회"처럼 보여도 저장 payload와 시트 write 값은 12, 2 같은 숫자로 정규화합니다.
++function normalizePortalContractUnitMonthsP280_(value) {
++  const text = String(value == null ? '' : value).trim();
++  if (!text || text === '-' || text === '–' || text === '—') return '';
++  if (/^1년$/.test(text)) return 12;
++  if (/^반년$/.test(text)) return 6;
++  const m = text.match(/\d+/);
++  if (!m) return '';
++  const n = Number(m[0]);
++  if (!isFinite(n) || n < 1 || n > 12) return '';
++  return n;
++}
++
++function normalizePortalInspectionCountP280_(value) {
++  const text = String(value == null ? '' : value).trim();
++  if (!text || text === '-' || text === '–' || text === '—') return '';
++  const m = text.match(/\d+/);
++  if (!m) return '';
++  const n = Number(m[0]);
++  if (!isFinite(n) || n < 0 || n > 12) return '';
++  return n;
++}
++
++function normalizePortalContractFieldForDbP280_(key, value) {
++  key = String(key || '').trim();
++  if (key === 'contractUnit') {
++    const months = normalizePortalContractUnitMonthsP280_(value);
++    return months === '' ? '' : String(months);
++  }
++  if (key === 'maintenance' || key === 'performance') {
++    const count = normalizePortalInspectionCountP280_(value);
++    return count === '' ? '' : String(count);
++  }
++  return value == null ? '' : String(value).trim();
++}
++
++function normalizePortalContractPayloadFieldsP280_(values) {
++  values = Object.assign({}, values || {});
++  ['contractUnit', 'maintenance', 'performance'].forEach(function(key) {
++    if (Object.prototype.hasOwnProperty.call(values, key)) {
++      values[key] = normalizePortalContractFieldForDbP280_(key, values[key]);
++    }
++  });
++  return values;
++}
++
++function getPortalMasterWriteValueP280_(key, value) {
++  key = String(key || '').trim();
++  if (key === 'contractUnit') {
++    const months = normalizePortalContractUnitMonthsP280_(value);
++    return months === '' ? '' : months;
++  }
++  if (key === 'maintenance' || key === 'performance') {
++    const count = normalizePortalInspectionCountP280_(value);
++    return count === '' ? '' : count;
++  }
++  return String(value == null ? '' : value).trim();
++}
++
++function getPortalMasterCompareTextP280_(key, value) {
++  const writeValue = getPortalMasterWriteValueP280_(key, value);
++  return String(writeValue == null ? '' : writeValue).trim();
++}
++
+ // PATCH P1-12: 계약조건 저장 검증 + 할인율 공란 0 보정
+ // - 변수 입력 부족이 시트 수식에 남지 않도록 계약 계산 필수값을 저장 전 차단합니다.
+ // - 할인율은 공란이면 0으로 강제 저장합니다.
+@@ -1690,7 +1759,7 @@
+ 
+ function preparePortalContractValuesForSaveP112_(values, options) {
+   options = options || {};
+-  values = Object.assign({}, values || {});
++  values = normalizePortalContractPayloadFieldsP280_(Object.assign({}, values || {}));
+   const requireFull = !!options.requireFull;
+   const shouldValidate = requireFull || isPortalContractSaveTouchedP112_(values);
+   if (!shouldValidate) return values;
+@@ -1746,6 +1815,9 @@
+   const maintenanceText = String(payload.maintenance || '').trim();
+   const performanceText = String(payload.performance || '').trim();
+   const vatText = String(payload.vat || '').trim();
++  const normalizedContractMonthsP280 = normalizePortalContractUnitMonthsP280_(contractUnitText);
++  const normalizedMaintenanceCountP280 = normalizePortalInspectionCountP280_(maintenanceText);
++  const normalizedPerformanceCountP280 = normalizePortalInspectionCountP280_(performanceText);
+ 
+   const anyInput = [grade, discountTextRaw, contractUnitText, appointmentText, maintenanceText, performanceText, vatText, String(payload.targetFinalPrice || '').trim(), String(payload.area || '').trim()]
+     .some(v => String(v || '').trim() !== '');
+@@ -1756,10 +1828,10 @@
+   const missing = [];
+   if (!(area > 0)) missing.push('연면적');
+   if (!grade) missing.push('관리등급');
+-  if (isPortalContractValueBlankP112_(contractUnitText)) missing.push('계약단위');
++  if (isPortalContractValueBlankP112_(contractUnitText) || normalizedContractMonthsP280 === '') missing.push('계약단위');
+   if (isPortalContractValueBlankP112_(appointmentText)) missing.push('관리자 선임 여부');
+-  if (isPortalContractValueBlankP112_(maintenanceText)) missing.push('유지점검');
+-  if (isPortalContractValueBlankP112_(performanceText)) missing.push('성능점검');
++  if (isPortalContractValueBlankP112_(maintenanceText) || normalizedMaintenanceCountP280 === '') missing.push('유지점검');
++  if (isPortalContractValueBlankP112_(performanceText) || normalizedPerformanceCountP280 === '') missing.push('성능점검');
+   if (isPortalContractValueBlankP112_(vatText)) missing.push('부가세');
+ 
+   if (missing.length) {
+@@ -1780,10 +1852,10 @@
+   const basis = getContractBasisForGrade_(grade);
+   if (!basis) throw new Error('계약기준 시트에서 관리등급 [' + (grade || '-') + '] 기준단가를 찾지 못했습니다.');
+ 
+-  const months = /12개월|1년/.test(contractUnitText) ? 12 : 6;
++  const months = normalizedContractMonthsP280 || 12;
+   const hasAppointment = appointmentText === '선임' || (appointmentText.indexOf('선임') >= 0 && appointmentText.indexOf('미선임') < 0 && appointmentText.indexOf('해당없음') < 0);
+-  const maintenanceCount = parseCount_(maintenanceText);
+-  const performanceCount = parseCount_(performanceText);
++  const maintenanceCount = normalizedMaintenanceCountP280 === '' ? 0 : normalizedMaintenanceCountP280;
++  const performanceCount = normalizedPerformanceCountP280 === '' ? 0 : normalizedPerformanceCountP280;
+ 
+   const appointmentAmount = hasAppointment ? basis.appointmentUnit * months : 0;
+   const maintenanceAmount = basis.maintenanceUnit * maintenanceCount;
+@@ -1813,7 +1885,7 @@
+     ok: true,
+     grade: grade,
+     months: months,
+-    contractUnit: months === 12 ? '12개월' : '6개월',
++    contractUnit: String(months),
+     hasAppointment: hasAppointment,
+     maintenanceCount: maintenanceCount,
+     performanceCount: performanceCount,
+@@ -1898,11 +1970,11 @@
+   };
+   setField('basic', 'firstRegisteredAt', todayText);
+   setField('basic', 'status', '');
+-  setField('contract', 'contractUnit', '12개월');
++  setField('contract', 'contractUnit', '12');
+   setField('contract', 'vat', '별도');
+   setField('contract', 'appointment', '선임');
+-  setField('contract', 'maintenance', '2회');
+-  setField('contract', 'performance', '1회');
++  setField('contract', 'maintenance', '2');
++  setField('contract', 'performance', '1');
+ 
+   return detail;
+ }
+@@ -1987,7 +2059,7 @@
+       col = ensureMasterColumn_(sheet, headerMap, headerName);
+       headerMap = getHeaderMap_(sheet);
+     }
+-    sheet.getRange(rowNo, col).setValue(String(merged[def.key] == null ? '' : merged[def.key]).trim());
++    sheet.getRange(rowNo, col).setValue(getPortalMasterWriteValueP280_(def.key, merged[def.key]));
+   });
+ 
+   SpreadsheetApp.flush();
+diff -ruN /mnt/data/step18_work/Component_CustomerDetail.html /mnt/data/step18_work_mod/Component_CustomerDetail.html
+--- /mnt/data/step18_work/Component_CustomerDetail.html	2026-06-28 03:45:26.000000000 +0000
++++ /mnt/data/step18_work_mod/Component_CustomerDetail.html	2026-06-28 05:21:09.590232656 +0000
+@@ -317,7 +317,7 @@
+     if (f.key === 'status') {
+       html += '<select class="detail-edit-input portal-status-select"' + data + ' disabled>' + renderStatusOptionsForDetail_(f.value || '') + '</select>';
+     } else {
+-      html += '<select class="detail-edit-input"' + data + ' disabled>' + renderGenericOptions(f.options || [], f.value || '') + '</select>';
++      html += '<select class="detail-edit-input"' + data + ' disabled>' + renderGenericOptions(f.options || [], f.value || '', f.key) + '</select>';
+     }
+   } else {
+     const inputType = f.type === 'email' ? 'email' : 'text';
+@@ -327,11 +327,46 @@
+   return html;
+ }
+ 
+-function renderGenericOptions(options, current) {
+-  options = options || []; current = current || '';
++function getGenericOptionValueP280_(opt) {
++  if (opt && typeof opt === 'object') return opt.value == null ? '' : String(opt.value);
++  return opt == null ? '' : String(opt);
++}
++
++function getGenericOptionLabelP280_(opt) {
++  if (opt && typeof opt === 'object') return opt.label == null ? getGenericOptionValueP280_(opt) : String(opt.label);
++  return opt == null || String(opt) === '' ? '-' : String(opt);
++}
++
++function normalizeDetailSelectCurrentP280_(key, value) {
++  const text = String(value == null ? '' : value).trim();
++  if (!text || text === '-' || text === '–' || text === '—') return '';
++  if (key === 'contractUnit') {
++    if (text === '1년') return '12';
++    if (text === '반년') return '6';
++    const m = text.match(/\d+/);
++    return m ? String(Math.max(1, Math.min(12, Number(m[0]) || 12))) : text;
++  }
++  if (key === 'maintenance' || key === 'performance') {
++    const m = text.match(/\d+/);
++    return m ? String(Math.max(0, Math.min(12, Number(m[0]) || 0))) : text;
++  }
++  return text;
++}
++
++function renderGenericOptions(options, current, key) {
++  options = (options || []).slice();
++  const normalizedCurrent = normalizeDetailSelectCurrentP280_(key || '', current);
+   const seen = {}; let html = '';
+-  if (current && options.indexOf(current) < 0) options.unshift(current);
+-  options.forEach(opt => { if (seen[opt]) return; seen[opt]=true; html += '<option value="' + escAttr(opt) + '"' + (String(opt)===String(current)?' selected':'') + '>' + esc(opt || '-') + '</option>'; });
++  const hasCurrent = options.some(function(opt) { return String(getGenericOptionValueP280_(opt)) === String(normalizedCurrent); });
++  if (normalizedCurrent && !hasCurrent) options.unshift({ value: normalizedCurrent, label: current || normalizedCurrent });
++  options.forEach(function(opt) {
++    const value = getGenericOptionValueP280_(opt);
++    const label = getGenericOptionLabelP280_(opt);
++    const seenKey = String(value);
++    if (seen[seenKey]) return;
++    seen[seenKey] = true;
++    html += '<option value="' + escAttr(value) + '"' + (String(value) === String(normalizedCurrent) ? ' selected' : '') + '>' + esc(label || '-') + '</option>';
++  });
+   return html;
+ }
+ 
+@@ -378,6 +413,36 @@
+ };
+ const DETAIL_CONTRACT_SAVE_KEYS_P112 = DETAIL_CONTRACT_REQUIRED_KEYS_P112.concat(['discountRate','grade','finalQuote','targetFinalPrice']);
+ 
++
++function normalizeDetailContractFieldForDbP280_(key, value) {
++  const text = String(value == null ? '' : value).trim();
++  if (key === 'contractUnit') {
++    if (!text || text === '-' || text === '–' || text === '—') return '';
++    if (text === '1년') return '12';
++    if (text === '반년') return '6';
++    const m = text.match(/\d+/);
++    if (!m) return text;
++    const n = Number(m[0]);
++    return n >= 1 && n <= 12 ? String(n) : '';
++  }
++  if (key === 'maintenance' || key === 'performance') {
++    if (!text || text === '-' || text === '–' || text === '—') return '';
++    const m = text.match(/\d+/);
++    if (!m) return text;
++    const n = Number(m[0]);
++    return n >= 0 && n <= 12 ? String(n) : '';
++  }
++  return text;
++}
++
++function normalizeDetailContractValuesForDbP280_(values) {
++  values = Object.assign({}, values || {});
++  ['contractUnit', 'maintenance', 'performance'].forEach(function(key) {
++    if (Object.prototype.hasOwnProperty.call(values, key)) values[key] = normalizeDetailContractFieldForDbP280_(key, values[key]);
++  });
++  return values;
++}
++
+ function isDetailContractValueBlankP112_(value) {
+   const text = String(value == null ? '' : value).trim();
+   if (!text) return true;
+@@ -393,12 +458,12 @@
+ 
+ function prepareDetailContractValuesForClientSaveP112_(values, options) {
+   options = options || {};
+-  values = Object.assign({}, values || {});
++  values = normalizeDetailContractValuesForDbP280_(Object.assign({}, values || {}));
+   const requireFull = !!options.requireFull;
+   if (!requireFull && !isDetailContractSaveTouchedP112_(values)) return values;
+ 
+   const currentValues = collectCurrentDetailInputValues_ ? collectCurrentDetailInputValues_() : gatherRegistrationValues();
+-  const effective = Object.assign({}, currentValues || {}, values || {});
++  const effective = normalizeDetailContractValuesForDbP280_(Object.assign({}, currentValues || {}, values || {}));
+   if (isDetailContractValueBlankP112_(effective.discountRate)) {
+     effective.discountRate = '0';
+     values.discountRate = '0';
+@@ -642,7 +707,7 @@
+     }
+   }
+ 
+-  setInputValueByKeyIfExists_('contractUnit', '12개월', { onlyBlank: true });
++  setInputValueByKeyIfExists_('contractUnit', '12', { onlyBlank: true });
+   updateRegionVendorFromAddress_({ onlyIfAddress: true, onlyBlankVendor: true, normalizeAddress: false });
+   enforceRegionReadonly_();
+ }
+@@ -759,9 +824,13 @@
+ 
+ function normalizeContractUnitValue_(value) {
+   const text = String(value || '').trim();
+-  if (text === '12개월' || text === '1년') return '12개월';
+-  if (text === '6개월' || text === '반년') return '6개월';
+-  return text;
++  if (!text) return '';
++  if (text === '1년') return '12';
++  if (text === '반년') return '6';
++  const m = text.match(/\d+/);
++  if (!m) return text;
++  const n = Number(m[0]);
++  return n >= 1 && n <= 12 ? String(n) : text;
+ }
+ 
+ function applyContractUnitDefaults_(contractUnit, options) {
+@@ -772,17 +841,17 @@
+     return;
+   }
+ 
+-  if (unit === '12개월') {
++  if (unit === '12') {
+     setInputValueByKeyIfExists_('appointment', '선임', options);
+-    setInputValueByKeyIfExists_('maintenance', '2회', options);
+-    setInputValueByKeyIfExists_('performance', '1회', options);
++    setInputValueByKeyIfExists_('maintenance', '2', options);
++    setInputValueByKeyIfExists_('performance', '1', options);
+     setInputValueByKeyIfExists_('vat', '별도', options.onlyBlank ? { onlyBlank: true } : options);
+     return;
+   }
+ 
+-  if (unit === '6개월') {
++  if (unit === '6') {
+     setInputValueByKeyIfExists_('appointment', '선임', options);
+-    setInputValueByKeyIfExists_('maintenance', '1회', options);
++    setInputValueByKeyIfExists_('maintenance', '1', options);
+     // 6개월은 성능점검 0회/1회가 모두 쓰이므로 미리 선택하지 않습니다.
+     setInputValueByKeyIfExists_('performance', '', options);
+     setInputValueByKeyIfExists_('vat', '별도', options.onlyBlank ? { onlyBlank: true } : options);
+@@ -794,7 +863,7 @@
+   setInputValueByKeyIfExists_('vat', '별도', { onlyBlank: true });
+   const unitEl = getDetailInputByKey_('contractUnit');
+   if (currentDetail && currentDetail.isNew && unitEl && !String(unitEl.value || '').trim()) {
+-    unitEl.value = '12개월';
++    unitEl.value = '12';
+   }
+   if (unitEl && String(unitEl.value || '').trim()) {
+     applyContractUnitDefaults_(unitEl.value, { onlyBlank: true });
+diff -ruN /mnt/data/step18_work/Component_CustomerSearch.html /mnt/data/step18_work_mod/Component_CustomerSearch.html
+--- /mnt/data/step18_work/Component_CustomerSearch.html	2026-06-28 03:45:26.000000000 +0000
++++ /mnt/data/step18_work_mod/Component_CustomerSearch.html	2026-06-28 05:21:58.234712953 +0000
+@@ -1453,6 +1453,14 @@
+   if (key === 'serviceApplicationReceived' || key === 'appointmentReportReceived') {
+     return normalizeCustomerExpandBooleanMark_(normalized);
+   }
++  if (key === 'contractUnit') {
++    const v = normalizePortalContractDisplayValueClientP280_('contractUnit', normalized);
++    return v ? v + '개월' : disp(normalized);
++  }
++  if (key === 'maintenance' || key === 'performance') {
++    const v = normalizePortalContractDisplayValueClientP280_(key, normalized);
++    return v !== '' ? v + '회' : disp(normalized);
++  }
+   if (key === 'phone' || key === 'directPhone') {
+     return splitCustomerExpandPhoneLines_(normalized) || disp(normalized);
+   }
+@@ -3006,6 +3014,42 @@
+ }
+ 
+ 
++
++function buildPortalContractUnitOptionsClientP280_() {
++  const opts = [{ value: '', label: '-' }];
++  for (let i = 1; i <= 12; i++) opts.push({ value: String(i), label: i + '개월' });
++  return opts;
++}
++
++function buildPortalInspectionCountOptionsClientP280_(maxCount) {
++  maxCount = Number(maxCount) || 12;
++  const opts = [{ value: '', label: '-' }];
++  for (let i = 0; i <= maxCount; i++) opts.push({ value: String(i), label: i + '회' });
++  return opts;
++}
++
++function normalizePortalContractDisplayValueClientP280_(key, value) {
++  const text = String(value == null ? '' : value).trim();
++  if (!text || text === '-' || text === '–' || text === '—') return '';
++  if (key === 'contractUnit') {
++    if (text === '1년') return '12';
++    if (text === '반년') return '6';
++    const m = text.match(/\d+/);
++    return m ? String(Math.max(1, Math.min(12, Number(m[0]) || 12))) : text;
++  }
++  if (key === 'maintenance' || key === 'performance') {
++    const m = text.match(/\d+/);
++    return m ? String(Math.max(0, Math.min(12, Number(m[0]) || 0))) : text;
++  }
++  return text;
++}
++
++function getPortalContractFieldOptionsClientP280_(key) {
++  if (key === 'contractUnit') return buildPortalContractUnitOptionsClientP280_();
++  if (key === 'maintenance' || key === 'performance') return buildPortalInspectionCountOptionsClientP280_(12);
++  return null;
++}
++
+ function findCustomerRowSummaryByCustomerNo_(customerNo) {
+   const cno = normalizeCustomerNoForKey_(customerNo);
+   if (!cno) return null;
+@@ -3056,10 +3100,10 @@
+     grade: row.grade || '',
+     buildingType: row.buildingType || '',
+     finalQuote: row.finalQuote || '',
+-    contractUnit: row.contractUnit || '',
++    contractUnit: normalizePortalContractDisplayValueClientP280_('contractUnit', row.contractUnit || ''),
+     appointment: row.appointment || '',
+-    maintenance: row.maintenance || '',
+-    performance: row.performance || '',
++    maintenance: normalizePortalContractDisplayValueClientP280_('maintenance', row.maintenance || ''),
++    performance: normalizePortalContractDisplayValueClientP280_('performance', row.performance || ''),
+     vat: row.vat || '',
+     discountRate: row.discountRate || '',
+     specialTerms: row.specialTerms || '',
+@@ -3099,8 +3143,12 @@
+     ['discountRate','할인율',true,'numberText'], ['specialTerms','용역신청서특약사항',true,'textarea',true]
+   ];
+   const make = function(def) {
+-    const field = { key:def[0], label:def[1], editable:def[2], type:def[3], wide:!!def[4], value: detail[def[0]] || '' };
++    const key = def[0];
++    const opts = getPortalContractFieldOptionsClientP280_(key);
++    const value = opts ? normalizePortalContractDisplayValueClientP280_(key, detail[key] || '') : (detail[key] || '');
++    const field = { key:key, label:def[1], editable:def[2], type:def[3], wide:!!def[4], value: value };
+     if (field.key === 'customerRank') field.options = ['', 'A급', 'B급', 'C급', '보류', '제외'];
++    if (opts) field.options = opts;
+     return field;
+   };
+   return { basic: basicDefs.map(make), contract: contractDefs.map(make) };
