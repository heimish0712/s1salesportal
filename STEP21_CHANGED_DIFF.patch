diff -ru step21_base/Component_CustomerSearch.html step21_work/Component_CustomerSearch.html
--- step21_base/Component_CustomerSearch.html	2026-06-28 06:43:52.000000000 +0000
+++ step21_work/Component_CustomerSearch.html	2026-06-28 08:05:19.123562692 +0000
@@ -162,6 +162,17 @@
   return true;
 }
 
+function canServeCustomerSearchFastP300_(keyword, page) {
+  try {
+    if (customerSearchIndexState && customerSearchIndexState.ready && Array.isArray(customerSearchIndexState.rows) && customerSearchIndexState.rows.length) return true;
+  } catch (err) {}
+  try {
+    return !!getCustomerPageFromCache_(keyword, page);
+  } catch (err) {
+    return false;
+  }
+}
+
 function searchCustomerPage(page) {
   ensureCustomerFilterUi_();
   renderCustomerScopeToggleP04_();
@@ -177,8 +188,11 @@
   const requestSeq = ++customerSearchRequestSeqP230;
   const requestSnapshot = makeCustomerSearchRequestSnapshotP230_(keywordValue, pageNo);
   updateCustomerFilterSummary_();
-  resultArea.innerHTML = '<div class="loading">고객 데이터를 불러오는 중입니다...<br><span class="cache-note">검색인덱스_DB를 먼저 조회하고 상세정보는 뒤에서 준비합니다.</span></div>';
-  initializeCustomerSearchIndex({ silent: true, useLocalFirst: true });
+  const canServeFast = canServeCustomerSearchFastP300_(keywordValue, pageNo);
+  if (!canServeFast) {
+    resultArea.innerHTML = '<div class="loading">고객 데이터를 불러오는 중입니다...<br><span class="cache-note">브라우저 캐시 또는 검색인덱스_DB를 확인합니다.</span></div>';
+  }
+  initializeCustomerSearchIndex({ silent: true, useLocalFirst: true, skipNetworkIfReady: true, renderAfterLoad: false });
 
   if (hasActiveCustomerFilterConditions_() && !(customerSearchIndexState && customerSearchIndexState.ready && Array.isArray(customerSearchIndexState.rows))) {
     waitCustomerFilterIndexThenSearch_(keywordValue, pageNo, requestSeq, requestSnapshot);
@@ -531,8 +545,35 @@
 
   const rowSummary = findCustomerRowSummaryByRowNo_(rowNo) || {};
   const detail = getCustomerDetailCache_(rowNo, rowSummary.customerNo) || null;
+
+  if (key === 'memo' && (!detail || detail.__summary || detail.__lite || !detail.__masterBacked)) {
+    cell.setAttribute('data-original-html', cell.innerHTML);
+    cell.classList.add('customer-inline-editing-cell');
+    cell.innerHTML = '<div class="customer-inline-memo-loading-p300">전체 메모를 불러오는 중입니다...</div>';
+    fetchCustomerDetailFromServer_(rowNo, function(fullDetail) {
+      if (!fullDetail || Number(fullDetail.rowNo) !== rowNo) return;
+      const currentCell = document.querySelector('td[data-row-no="' + rowNo + '"][data-key="memo"]') || cell;
+      const originalHtml = currentCell.getAttribute('data-original-html');
+      if (originalHtml != null) currentCell.innerHTML = originalHtml;
+      currentCell.removeAttribute('data-original-html');
+      currentCell.classList.remove('customer-inline-editing-cell');
+      const fakeEvent = {
+        __customerInlineCell: currentCell,
+        currentTarget: currentCell,
+        stopPropagation: function(){},
+        preventDefault: function(){}
+      };
+      startCustomerInlineEdit_(fakeEvent, rowNo, 'memo');
+    }, function(err) {
+      cell.innerHTML = cell.getAttribute('data-original-html') || '<div class="customer-cell-memo">전체 메모를 불러오지 못했습니다.</div>';
+      cell.classList.remove('customer-inline-editing-cell');
+      alert('전체 메모를 불러오지 못했습니다: ' + getErrorMessage_(err));
+    });
+    return;
+  }
+
   let currentValue = '';
-  if (key === 'memo') currentValue = detail && detail.memo != null ? detail.memo : rowSummary.memo;
+  if (key === 'memo') currentValue = detail && detail.memo != null ? detail.memo : '';
   else currentValue = detail && detail[key] != null ? detail[key] : rowSummary[key];
   currentValue = String(currentValue == null ? '' : currentValue);
 
@@ -1392,14 +1433,16 @@
     ]
   ], detail, 'extra', 'customer-expand-block-biz');
 
-  const memoValue = getCustomerExpandFieldValue_(detail, 'basic', 'memo').value || detail.memo || rowSummary.memo || '';
+  const isFullMemoReadyP300 = !!(detail && detail.__masterBacked && !detail.__summary && !detail.__lite);
+  const memoValue = isFullMemoReadyP300 ? (getCustomerExpandFieldValue_(detail, 'basic', 'memo').value || detail.memo || '') : '';
   const expandedMemoInputId = 'expandedMemo_' + rowNo;
   html += '<div class="customer-expand-form-block customer-expand-memo-box-v58" id="expandedMemoBox_' + rowNo + '">' +
     '<div class="customer-expand-memo-label-v58">마스터시트<br>메모</div>' +
     '<div class="customer-expand-memo-main-v58">' +
+      (!isFullMemoReadyP300 ? '<div class="customer-expand-memo-loading-p300">전체 메모를 불러오는 중입니다. 요약 메모는 저장/수정 화면에 사용하지 않습니다.</div>' : '') +
       '<div class="customer-expand-memo-edit-wrap-p15 customer-expand-memo-edit-wrap-fixed-tags">' +
         '<div class="customer-expand-memo-tag-row customer-expand-memo-tag-row-always">' + renderCustomerMemoTagPopover_(expandedMemoInputId, 'customer-expand-memo-tag-popover') + '</div>' +
-        '<textarea class="customer-expand-memo" id="expandedMemo_' + rowNo + '">' + esc(memoValue) + '</textarea>' +
+        '<textarea class="customer-expand-memo" id="expandedMemo_' + rowNo + '"' + (!isFullMemoReadyP300 ? ' readonly data-memo-loading="Y" placeholder="전체 메모 불러오는 중"' : '') + '>' + esc(memoValue) + '</textarea>' +
       '</div>' +
       '<div class="customer-expand-save-row">' +
         '<button type="button" class="secondary" onclick="event.stopPropagation(); resetExpandedCustomerMemo(' + rowNo + ')">되돌리기</button>' +
@@ -1804,6 +1847,10 @@
   const memoEl = document.getElementById('expandedMemo_' + rowNo);
   const btn = document.getElementById('expandedMemoSaveBtn_' + rowNo);
   if (!memoEl) return;
+  if (memoEl.getAttribute('data-memo-loading') === 'Y' || memoEl.readOnly) {
+    toast('전체 메모를 불러온 뒤 저장할 수 있습니다.');
+    return;
+  }
 
   const memoValue = memoEl.value || '';
   const prevRow = findCustomerRowSummaryByRowNo_(rowNo);
@@ -2003,6 +2050,14 @@
     loadCustomerSearchIndexFromLocalStorage_();
   }
 
+  const readyRowsP300 = !!(customerSearchIndexState && customerSearchIndexState.ready && Array.isArray(customerSearchIndexState.rows) && customerSearchIndexState.rows.length);
+  const loadedAtP300 = Number(customerSearchIndexState && customerSearchIndexState.loadedAt || 0);
+  const ageP300 = loadedAtP300 ? (Date.now() - loadedAtP300) : Number.MAX_SAFE_INTEGER;
+  const maxAgeP300 = Number(options.maxAgeMs || CUSTOMER_INDEX_BACKGROUND_REFRESH_MS_P300);
+  if (!force && readyRowsP300 && (options.skipNetworkIfReady || ageP300 < maxAgeP300)) {
+    return;
+  }
+
   if (customerSearchIndexState.loading) {
     if (force) customerSearchIndexForceQueuedP230 = true;
     return;
@@ -2031,7 +2086,7 @@
 
       setCustomerSearchIndexData_(data, 'server');
       if (!options.silent) toast('검색인덱스_DB를 새로 불러왔습니다.');
-      if (customerSearchState) {
+      if (options.renderAfterLoad === true && customerSearchState) {
         const current = buildCustomerPageFromIndex_(customerSearchState.keyword || '', customerSearchState.page || 0);
         if (current && document.getElementById('customerSearchSection') && document.getElementById('customerSearchSection').classList.contains('active')) {
           renderResults(current, true);
@@ -3089,7 +3144,11 @@
     customerRank: row.customerRank || '',
     statusOptions: [],
     customerRankOptions: ['', 'A급', 'B급', 'C급', '보류', '제외'],
-    memo: row.memo || '',
+    // 검색인덱스_DB의 메모는 목록 표시용 요약값일 수 있습니다.
+    // 상세/펼침/메모수정 화면에서는 잘린 메모를 보여주거나 저장하면 안 되므로,
+    // 마스터 상세 조회가 끝나기 전까지는 빈 값으로 두고 full detail 도착 후 채웁니다.
+    memo: '',
+    memoPreview: row.memo || '',
     lastSent: row.lastSent || '',
     sentAt: row.sentAt || '',
 
diff -ru step21_base/Component_FocusCustomer.html step21_work/Component_FocusCustomer.html
--- step21_base/Component_FocusCustomer.html	2026-06-28 06:44:25.000000000 +0000
+++ step21_work/Component_FocusCustomer.html	2026-06-28 08:04:55.825949644 +0000
@@ -10,14 +10,20 @@
   includeExcluded: false,
   sortMode: 'priority',
   loading: false,
-  data: null
+  data: null,
+  requestSeq: 0,
+  cache: {}
 };
 
 function loadFocusCustomerPageP290(forceRefresh) {
   const area = document.getElementById('focusCustomerArea');
   if (!area) return;
-  if (!focusCustomerStateP290_.data || forceRefresh) {
-    area.innerHTML = '<div class="loading">중점고객관리 데이터를 불러오는 중입니다.</div>';
+  if (!document.getElementById('focusListP290')) {
+    renderFocusCustomerShellP290_();
+  }
+  if (!focusCustomerStateP290_.data && !forceRefresh) {
+    const list = document.getElementById('focusListP290');
+    if (list) list.innerHTML = '<div class="loading">중점고객관리 데이터를 불러오는 중입니다.</div>';
   }
   requestFocusCustomerDataP290_(!!forceRefresh);
 }
@@ -39,10 +45,15 @@
   focusCustomerStateP290_.sortMode = sortMode ? sortMode.value || 'priority' : 'priority';
 }
 
+function makeFocusCustomerClientCacheKeyP300_(payload) {
+  try { return JSON.stringify(payload || {}); } catch (err) { return String(Date.now()); }
+}
+
 function requestFocusCustomerDataP290_(forceRefresh) {
   collectFocusCustomerFiltersP290_();
   const area = document.getElementById('focusCustomerArea');
   if (!area) return;
+  const requestSeq = ++focusCustomerStateP290_.requestSeq;
   focusCustomerStateP290_.loading = true;
   const btn = document.getElementById('focusRefreshBtnP290');
   if (btn) { btn.disabled = true; btn.textContent = forceRefresh ? '갱신 중' : '조회 중'; }
@@ -57,18 +68,31 @@
     page: focusCustomerStateP290_.page,
     pageSize: focusCustomerStateP290_.pageSize
   };
+  const cacheKey = makeFocusCustomerClientCacheKeyP300_(payload);
+  if (!forceRefresh && focusCustomerStateP290_.cache && focusCustomerStateP290_.cache[cacheKey]) {
+    focusCustomerStateP290_.data = focusCustomerStateP290_.cache[cacheKey];
+    renderFocusCustomerDataP290_(focusCustomerStateP290_.data, { fromClientCache: true });
+    focusCustomerStateP290_.loading = false;
+    if (btn) { btn.disabled = false; btn.textContent = '새로고침'; }
+    return;
+  }
   google.script.run
     .withSuccessHandler(function(res) {
+      if (requestSeq !== focusCustomerStateP290_.requestSeq) return;
       focusCustomerStateP290_.loading = false;
       if (btn) { btn.disabled = false; btn.textContent = '새로고침'; }
       if (forceRefresh && typeof toast === 'function') toast('중점고객관리 데이터를 갱신했습니다.');
       focusCustomerStateP290_.data = res || {};
+      focusCustomerStateP290_.cache = focusCustomerStateP290_.cache || {};
+      focusCustomerStateP290_.cache[cacheKey] = focusCustomerStateP290_.data;
       renderFocusCustomerDataP290_(res || {});
     })
     .withFailureHandler(function(err) {
+      if (requestSeq !== focusCustomerStateP290_.requestSeq) return;
       focusCustomerStateP290_.loading = false;
       if (btn) { btn.disabled = false; btn.textContent = '새로고침'; }
-      area.innerHTML = '<div class="loading" style="color:#b42318;">중점고객관리 데이터를 불러오지 못했습니다.<br>' + esc(getErrorMessage_(err)) + '</div>';
+      const list = document.getElementById('focusListP290') || area;
+      list.innerHTML = '<div class="loading" style="color:#b42318;">중점고객관리 데이터를 불러오지 못했습니다.<br>' + esc(getErrorMessage_(err)) + '</div>';
     })
     .getFocusCustomerDashboardP290(payload);
 }
@@ -93,7 +117,7 @@
 }
 
 function renderFocusCustomerDataP290_(data) {
-  renderFocusCustomerShellP290_();
+  if (!document.getElementById('focusListP290')) renderFocusCustomerShellP290_();
   hydrateFocusFilterOptionsP290_(data || {});
   restoreFocusFilterValuesP290_();
   renderFocusStatsP290_(data || {});
@@ -222,6 +246,7 @@
 
 function refreshFocusCustomersP290() {
   focusCustomerStateP290_.page = 0;
+  focusCustomerStateP290_.cache = {};
   google.script.run.withSuccessHandler(function(){ requestFocusCustomerDataP290_(true); }).withFailureHandler(function(){ requestFocusCustomerDataP290_(true); }).clearFocusCustomerCacheP290();
 }
 
Only in step21_work: STEP21_CHANGED_DIFF.patch
Only in step21_work: STEP21_STATIC_CHECK.json
diff -ru step21_base/Styles.html step21_work/Styles.html
--- step21_base/Styles.html	2026-06-28 06:43:53.000000000 +0000
+++ step21_work/Styles.html	2026-06-28 08:06:09.546281284 +0000
@@ -4595,4 +4595,35 @@
   .focus-today-list-p290 { grid-template-columns: 1fr; }
 }
 
+
+/* STEP21/P300: 메모는 목록 셀에서만 요약 표시합니다.
+   메모 수정/상세팝업/펼침 패널에서는 검색인덱스의 잘린 메모를 쓰지 않고,
+   마스터 상세 메모가 도착할 때까지 로딩 상태로 둡니다. */
+.customer-inline-memo-loading-p300,
+.customer-expand-memo-loading-p300 {
+  padding: 10px 12px;
+  border: 1px solid #d8e6fb;
+  border-radius: 8px;
+  background: #f0f7ff;
+  color: #174ea6;
+  font-size: 12px;
+  font-weight: 800;
+  line-height: 1.45;
+  white-space: normal;
+}
+.customer-expand-memo-box-v58 .customer-expand-memo[readonly][data-memo-loading="Y"] {
+  color: #667085;
+  background: #f8fafc;
+  border-style: dashed;
+}
+.customer-inline-memo-editor,
+.customer-expand-memo-box-v58 .customer-expand-memo-main-v58 .customer-expand-memo,
+.master-memo-input,
+.detail-edit-input[data-key="memo"] {
+  white-space: pre-wrap !important;
+  overflow-wrap: anywhere !important;
+  word-break: break-word !important;
+  text-overflow: clip !important;
+}
+
 </style>
