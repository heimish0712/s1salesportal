diff -ruN portal_step10_base_for_step11/00_Config.gs portal_step11_work/00_Config.gs
--- portal_step10_base_for_step11/00_Config.gs	2026-06-24 19:22:14.000000000 +0000
+++ portal_step11_work/00_Config.gs	2026-06-27 03:49:03.007740144 +0000
@@ -162,7 +162,7 @@
 
 const PORTAL_SUPPORT_REQUEST_TYPES = ['수기견적', '자료제출', '약속잡기', '확인하기', '기타요청'];
 
-const PORTAL_SUPPORT_REQUESTER_OPTIONS = ['김경아', '최보람', '이옥희', '김서하', '방수원'];
+const PORTAL_SUPPORT_REQUESTER_OPTIONS = ['문형진', '방수원', '박새봄', '김경아', '최보람', '이옥희'];
 
 const PORTAL_SUPPORT_STATUS_OPTIONS = ['접수', '처리중', '완료', '보류', '반려'];
 
diff -ruN portal_step10_base_for_step11/09_SupportRequestService.gs portal_step11_work/09_SupportRequestService.gs
--- portal_step10_base_for_step11/09_SupportRequestService.gs	2026-06-27 01:26:17.000000000 +0000
+++ portal_step11_work/09_SupportRequestService.gs	2026-06-27 03:51:15.771193508 +0000
@@ -9,17 +9,49 @@
 // 이 파일에서는 PORTAL_SUPPORT_REQUEST_TYPES / PORTAL_SUPPORT_REQUESTER_OPTIONS /
 // PORTAL_SUPPORT_STATUS_OPTIONS / PORTAL_SUPPORT_MEMO_HEADER를 절대 재선언하지 않습니다.
 
-function buildPortalSupportRequesterOptions_(currentUser) {
+function getPortalSupportDelegatedRequesterNamesP250_() {
+  return (PORTAL_SUPPORT_REQUESTER_OPTIONS || ['문형진', '방수원', '박새봄', '김경아', '최보람', '이옥희']).slice();
+}
+
+function canPortalChooseDelegatedSupportRequesterP250_(perm) {
+  perm = perm || getPortalCurrentPermission_();
+  return !!(perm && perm.active !== false && (perm.canUseAdminHome || perm.canCompleteSupport || perm.isAdmin));
+}
+
+function buildPortalSupportRequesterOptions_(currentUser, perm) {
+  perm = perm || getPortalCurrentPermission_();
+  currentUser = String(currentUser || getPortalCurrentUserName_() || '').trim();
   const out = [];
   function add(v) {
     v = String(v || '').trim();
     if (v && out.indexOf(v) < 0) out.push(v);
   }
-  add(currentUser || getPortalCurrentUserName_());
-  (PORTAL_SUPPORT_REQUESTER_OPTIONS || []).forEach(add);
+
+  if (canPortalChooseDelegatedSupportRequesterP250_(perm)) {
+    getPortalSupportDelegatedRequesterNamesP250_().forEach(add);
+    add(currentUser);
+  } else {
+    add(currentUser);
+  }
   return out;
 }
 
+function assertPortalSupportRequesterAllowedP250_(requester, perm) {
+  perm = perm || getPortalCurrentPermission_();
+  requester = String(requester || '').trim();
+  const currentUser = getPortalCurrentUserName_();
+  const options = buildPortalSupportRequesterOptions_(currentUser, perm);
+  const normalizedRequester = normalizePortalNameForPermission_(requester);
+  const ok = options.some(function(v) {
+    return normalizePortalNameForPermission_(v) === normalizedRequester;
+  });
+  if (ok) return true;
+  if (canPortalChooseDelegatedSupportRequesterP250_(perm)) {
+    throw new Error('요청자는 지정된 요청자 목록에서만 선택할 수 있습니다.');
+  }
+  throw new Error('요청자는 본인만 선택할 수 있습니다.');
+}
+
 
 // v64: 영업지원요청 목록 조회 속도 개선용 서버 캐시/읽기 전용 시트 접근
 // - 목록 조회 때마다 헤더 보정/서식 쓰기를 하지 않습니다.
@@ -121,7 +153,7 @@
     requestTypes: PORTAL_SUPPORT_REQUEST_TYPES,
     statusOptions: PORTAL_SUPPORT_STATUS_OPTIONS,
     currentUser: getPortalCurrentUserName_(),
-    requesterOptions: buildPortalSupportRequesterOptions_(getPortalCurrentUserName_()),
+    requesterOptions: buildPortalSupportRequesterOptions_(getPortalCurrentUserName_(), perm),
     permission: sanitizePortalPermissionForClient_(perm),
     meta: { mode: 'full', recent: false, complete: true, limit: limit, keyword: keyword, status: statusFilter, loadedAt: new Date().toISOString() }
   };
@@ -347,7 +379,7 @@
     requestTypes: PORTAL_SUPPORT_REQUEST_TYPES,
     statusOptions: PORTAL_SUPPORT_STATUS_OPTIONS,
     currentUser: getPortalCurrentUserName_(),
-    requesterOptions: buildPortalSupportRequesterOptions_(getPortalCurrentUserName_()),
+    requesterOptions: buildPortalSupportRequesterOptions_(getPortalCurrentUserName_(), perm),
     permission: sanitizePortalPermissionForClient_(perm),
     meta: meta
   };
@@ -356,7 +388,7 @@
 // v78: 상세/처리 팝업 빠른 로딩용 상세 캐시 API
 // - 목록 행에서 즉시 팝업을 채우고, 서버 상세는 ScriptCache를 먼저 봅니다.
 // - 저장 시 bumpPortalSupportCacheBustV64_()가 호출되어 목록/상세 캐시가 같이 무효화됩니다.
-const PORTAL_SUPPORT_DETAIL_CACHE_PREFIX_V78 = 'supportDetail:v78:';
+const PORTAL_SUPPORT_DETAIL_CACHE_PREFIX_V78 = 'supportDetail:v250:';
 const PORTAL_SUPPORT_DETAIL_CACHE_TTL_SEC_V78 = 300;
 
 function makePortalSupportDetailCacheKeyV78_(rowNo) {
@@ -379,8 +411,12 @@
       if (cached) {
         const parsed = JSON.parse(cached);
         if (parsed && parsed.item) {
+          const perm = getPortalCurrentPermission_();
           parsed.fromCache = true;
           parsed.loadedAt = parsed.loadedAt || new Date().toISOString();
+          parsed.currentUser = getPortalCurrentUserName_();
+          parsed.requesterOptions = buildPortalSupportRequesterOptions_(parsed.currentUser, perm);
+          parsed.permission = sanitizePortalPermissionForClient_(perm);
           return parsed;
         }
       }
@@ -393,13 +429,14 @@
   const item = buildPortalSupportRowObject_(values, rowNo, headerMap);
   if (!item || (!item.requestText && !item.customerNo && !item.customerName)) throw new Error('영업지원 요청을 찾지 못했습니다.');
 
+  const perm = getPortalCurrentPermission_();
   const result = {
     item: item,
     requestTypes: PORTAL_SUPPORT_REQUEST_TYPES,
     statusOptions: PORTAL_SUPPORT_STATUS_OPTIONS,
     currentUser: getPortalCurrentUserName_(),
-    requesterOptions: buildPortalSupportRequesterOptions_(getPortalCurrentUserName_()),
-    permission: sanitizePortalPermissionForClient_(getPortalCurrentPermission_()),
+    requesterOptions: buildPortalSupportRequesterOptions_(getPortalCurrentUserName_(), perm),
+    permission: sanitizePortalPermissionForClient_(perm),
     fromCache: false,
     loadedAt: new Date().toISOString()
   };
@@ -592,6 +629,7 @@
 
   if (!requestType) throw new Error('업무유형을 선택하세요.');
   if (!requester) throw new Error('요청자를 입력하세요.');
+  assertPortalSupportRequesterAllowedP250_(requester, permForSupport);
   if (!requestText) throw new Error('요청업무를 입력하세요.');
 
   const receiptNo = String(payload.receiptNo || '').trim() || getExistingOrNextPortalSupportReceiptNo_(sheet, headerMap, rowNo);
diff -ruN portal_step10_base_for_step11/15_PermissionService.gs portal_step11_work/15_PermissionService.gs
--- portal_step10_base_for_step11/15_PermissionService.gs	2026-06-27 02:51:37.000000000 +0000
+++ portal_step11_work/15_PermissionService.gs	2026-06-27 03:50:19.170230309 +0000
@@ -44,7 +44,7 @@
 
 // STEP6 P230: 권한_DB 반복 조회를 줄이기 위한 짧은 서버 캐시입니다.
 // 권한 정책은 그대로 두고, 같은 사용자 이메일의 계산 완료 권한 객체만 5분 이내 재사용합니다.
-const PORTAL_PERMISSION_CACHE_PREFIX_P230 = 'PORTAL_PERMISSION_P230_';
+const PORTAL_PERMISSION_CACHE_PREFIX_P230 = 'PORTAL_PERMISSION_P250_';
 const PORTAL_PERMISSION_CACHE_BUST_PROP_P230 = 'PORTAL_PERMISSION_CACHE_BUST_P230';
 const PORTAL_PERMISSION_CACHE_TTL_SEC_P230 = 300;
 const PORTAL_PERMISSION_GUEST_CACHE_TTL_SEC_P230 = 60;
@@ -324,11 +324,14 @@
   // - 영업지원완료처리: 영업지원 처리/완료 입력 권한
   // - 작업로그전체열람/작업로그비교통계: 작업로그 전용 권한
   // SALES 계정에 전체고객열람=Y가 있어도 HOME/작업로그/완료처리는 관리자 권한으로 보지 않습니다.
+  const active = String(v(14) || 'Y').toUpperCase() !== 'N';
   const baseAdmin = isPortalAdminPermissionValues_(level, role, rank, defaultScope);
   const canViewAllCustomers = baseAdmin || yn_(v(8)) || defaultScope === 'ALL';
   const canWriteNotice = baseAdmin || yn_(v(9));
   const canDeleteNotice = baseAdmin || yn_(v(10));
-  const canWriteSupport = baseAdmin || yn_(v(11));
+  // STEP11 P250: 영업지원 요청 작성은 모든 등록/활성 사용자에게 허용합니다.
+  // 완료 처리/전체 열람 권한은 기존 권한_DB 정책을 그대로 유지합니다.
+  const canWriteSupport = active;
   const canReadAllSupport = baseAdmin || yn_(v(12));
   const canCompleteSupport = baseAdmin || yn_(v(13));
   const canViewAllActivityLogs = baseAdmin || yn_(v(16));
@@ -355,7 +358,7 @@
     canWriteSupport: canWriteSupport,
     canReadAllSupport: canReadAllSupport,
     canCompleteSupport: canCompleteSupport,
-    active: String(v(14) || 'Y').toUpperCase() !== 'N',
+    active: active,
     note: v(15),
     canViewAllActivityLogs: canViewAllActivityLogs,
     canCompareActivityLogs: canCompareActivityLogs
@@ -610,7 +613,7 @@
 
 function assertPortalCanWriteSupport_() {
   const perm = getPortalCurrentPermission_();
-  if (perm.canWriteSupport) return true;
+  if (perm && perm.active !== false && perm.canWriteSupport) return true;
   throw new Error('영업지원 요청 작성 권한이 없습니다.');
 }
 
diff -ruN portal_step10_base_for_step11/Component_Send.html portal_step11_work/Component_Send.html
--- portal_step10_base_for_step11/Component_Send.html	2026-06-27 01:38:42.000000000 +0000
+++ portal_step11_work/Component_Send.html	2026-06-27 03:49:03.010867042 +0000
@@ -92,6 +92,7 @@
         '<button type="button" class="send-file-preset-btn" onclick="applySendFileQuickPreset_(&quot;first&quot;)">#최초</button>' +
         '<button type="button" class="send-file-preset-btn" onclick="applySendFileQuickPreset_(&quot;required&quot;)">#필수</button>' +
         '<button type="button" class="send-file-preset-btn" onclick="applySendFileQuickPreset_(&quot;contractor&quot;)">#수행사</button>' +
+        '<button type="button" class="send-file-preset-btn send-file-clear-btn" onclick="clearAllSendFileChecksP250_()">모든 체크 해제</button>' +
       '</div>' +
     '</div>';
 }
@@ -126,6 +127,18 @@
   renderSendPreview();
 }
 
+function clearAllSendFileChecksP250_() {
+  document.querySelectorAll('.send-file-check').forEach(function(el) {
+    el.checked = false;
+  });
+  sendSelectedFileKeysV50 = [];
+  clearSendReviewSessionV75_('all file checks cleared');
+  captureSendGlobalsToWorkspaceP211_(getActiveSendWorkspaceP211_());
+  renderSendPreview();
+  renderSendWorkspaceTabsP211_();
+  toast('발송자료 체크를 모두 해제했습니다.');
+}
+
 // PATCH N: 견적/자료 발송 고객검색은 화면 부담을 줄이기 위해 5건 단위로 표시합니다.
 // - 고객 상세 검색 화면의 20건 페이지와 분리하여, 발송 화면 전용 페이지 크기를 사용합니다.
 // - 검색인덱스_DB가 준비되어 있으면 브라우저 인덱스에서 즉시 5건 단위로 표시하고,
@@ -654,6 +667,7 @@
       const base = buildSendWorkspaceP211_(idx + 1);
       const merged = Object.assign(base, ws || {});
       merged.order = idx + 1;
+      merged.createdAt = Number(merged.createdAt || (savedAt ? savedAt + idx : Date.now() + idx));
       merged.mailProgressPollTimer = null;
       merged.recipientTouched = Object.assign({ to:false, cc:false }, merged.recipientTouched || {});
       merged.searchState = Object.assign({ keyword:'', page:0, pageSize:SEND_CUSTOMER_PAGE_SIZE }, merged.searchState || {});
@@ -685,6 +699,7 @@
   return {
     id: 'send_ws_' + (Date.now()) + '_' + (sendWorkspaceSeedP211_++),
     order: Number(order || 1),
+    createdAt: Date.now(),
     title: '발송고객 ' + Number(order || 1),
     customerName: '',
     searchState: { keyword: '', page: 0, pageSize: SEND_CUSTOMER_PAGE_SIZE },
@@ -1196,14 +1211,15 @@
   });
 }
 
-function selectSendCustomer(rowNo) {
+function selectSendCustomer(rowNo, options) {
+  options = options || {};
   const ws = getActiveSendWorkspaceP211_();
   if (!ws) return;
   applySendWorkspaceToGlobalsP211_(ws);
   rowNo = Number(rowNo) || 0;
   const summary = findCustomerRowSummaryByRowNo_(rowNo) || {};
   const targetCustomerNo = normalizeCustomerNoForKey_(summary.customerNo || (sendSelectedDetail && sendSelectedDetail.customerNo) || sendSelectedCustomerNoV50);
-  const dupWs = findSendWorkspaceByCustomerNoP212_(targetCustomerNo, ws.id);
+  const dupWs = options.skipDuplicateCheck ? null : findSendWorkspaceByCustomerNoP212_(targetCustomerNo, ws.id);
   if (dupWs) {
     toast('이미 다른 발송 작업창에서 선택한 고객입니다. 해당 탭으로 이동합니다.');
     activateSendWorkspaceTabP211_(dupWs.id);
@@ -1651,11 +1667,58 @@
   });
 }
 
+function resetSendWorkspaceForExternalOpenP250_(ws) {
+  if (!ws) return null;
+  const oldOrder = Number(ws.order || 1) || 1;
+  try { if (ws.mailProgressPollTimer) clearTimeout(ws.mailProgressPollTimer); } catch (err) {}
+  const next = buildSendWorkspaceP211_(oldOrder);
+  Object.keys(ws).forEach(function(k) { delete ws[k]; });
+  Object.assign(ws, next, { order: oldOrder, title: '발송고객 ' + oldOrder, customerName: '' });
+  return ws;
+}
+
+function getOldestSendWorkspaceForReuseP250_() {
+  ensureSendWorkspacesP211_();
+  if (!sendWorkspacesP211_.length) return null;
+  return sendWorkspacesP211_.slice().sort(function(a, b) {
+    const at = Number(a && a.createdAt || 0) || Number(a && a.order || 0) || 0;
+    const bt = Number(b && b.createdAt || 0) || Number(b && b.order || 0) || 0;
+    if (at !== bt) return at - bt;
+    return (Number(a && a.order || 0) || 0) - (Number(b && b.order || 0) || 0);
+  })[0] || null;
+}
+
+function createSendWorkspaceForCustomerOpenP250_() {
+  ensureSendWorkspacesP211_();
+  const current = getActiveSendWorkspaceP211_();
+  if (current) captureSendGlobalsToWorkspaceP211_(current);
+
+  let ws;
+  if (sendWorkspacesP211_.length < SEND_WORKSPACE_MAX_P211_) {
+    ws = buildSendWorkspaceP211_(sendWorkspacesP211_.length + 1);
+    sendWorkspacesP211_.push(ws);
+  } else {
+    ws = resetSendWorkspaceForExternalOpenP250_(getOldestSendWorkspaceForReuseP250_());
+    toast('발송 작업창이 5개라 가장 오래된 작업창을 새 고객으로 교체합니다.');
+  }
+
+  sendWorkspacesP211_.forEach(function(item, i) {
+    item.order = i + 1;
+    if (!item.customerName) item.title = '발송고객 ' + (i + 1);
+  });
+  sendActiveWorkspaceIdP211_ = ws ? ws.id : '';
+  persistSendWorkspacesP213_(true);
+  return ws;
+}
+
 function openSendForCustomer(rowNo) {
   switchPage('send', true);
   setTimeout(function(){
-    if (!getActiveSendWorkspaceP211_()) ensureSendWorkspacesP211_();
-    selectSendCustomer(Number(rowNo));
+    const ws = createSendWorkspaceForCustomerOpenP250_();
+    if (!ws) return;
+    renderSendWorkspaceTabsP211_();
+    renderSendWorkspaceBodyP211_();
+    selectSendCustomer(Number(rowNo), { skipDuplicateCheck: true });
   }, 120);
 }
 
diff -ruN portal_step10_base_for_step11/Component_Support.html portal_step11_work/Component_Support.html
--- portal_step10_base_for_step11/Component_Support.html	2026-06-27 01:38:42.000000000 +0000
+++ portal_step11_work/Component_Support.html	2026-06-27 03:49:03.009932055 +0000
@@ -345,27 +345,48 @@
 }
 
 function getSupportRequesterOptions_() {
-  const base = ['김경아','최보람','이옥희','김서하','방수원'];
   const current = (supportCurrentData && supportCurrentData.currentUser) || '';
   const fromServer = (supportCurrentData && supportCurrentData.requesterOptions) || [];
   const out = [];
   function add(v) { v = String(v || '').trim(); if (v && out.indexOf(v) < 0) out.push(v); }
-  add(current);
   fromServer.forEach(add);
-  base.forEach(add);
+  add(current);
   return out;
 }
 
+function renderSupportRequesterOptionsHtml_(selected) {
+  const options = getSupportRequesterOptions_();
+  selected = String(selected || '').trim();
+  if (selected && options.indexOf(selected) < 0) options.unshift(selected);
+  return options.map(function(v) {
+    const sel = String(v || '') === selected ? ' selected' : '';
+    return '<option value="' + escAttr(v) + '"' + sel + '>' + esc(v) + '</option>';
+  }).join('');
+}
+
+function fillSupportRequesterSelect_(id, selected) {
+  const el = document.getElementById(id);
+  if (!el) return;
+  const current = String(selected || (supportCurrentData && supportCurrentData.currentUser) || '').trim();
+  if (el.tagName && String(el.tagName).toUpperCase() === 'SELECT') {
+    el.innerHTML = renderSupportRequesterOptionsHtml_(current);
+    if (current) el.value = current;
+  } else {
+    el.value = current;
+    const options = getSupportRequesterOptions_();
+    if (options.length <= 1) el.readOnly = true;
+  }
+}
+
 function initSupportInlineEntryDefaults_() {
-  const requester = document.getElementById('supportInlineRequester');
-  if (requester && supportCurrentData && supportCurrentData.currentUser) requester.value = supportCurrentData.currentUser;
+  fillSupportRequesterSelect_('supportInlineRequester', (supportCurrentData && supportCurrentData.currentUser) || '');
   window.__supportInlineCustomer = null;
 }
 
 function resetSupportInlineEntry() {
   ['supportInlineCustomerNo','supportInlineCustomerName','supportInlineRequestText'].forEach(function(id){ const el=document.getElementById(id); if(el) el.value=''; });
   const type = document.getElementById('supportInlineRequestType'); if (type) type.value = '';
-  const requester = document.getElementById('supportInlineRequester'); if (requester && supportCurrentData && supportCurrentData.currentUser) requester.value = supportCurrentData.currentUser;
+  fillSupportRequesterSelect_('supportInlineRequester', (supportCurrentData && supportCurrentData.currentUser) || '');
   const summary = document.getElementById('supportInlineContractSummary');
   if (summary) { summary.textContent = '고객번호 입력 후 적용을 누르면 계약조건과 고객 정보가 자동으로 표시됩니다.'; summary.className = 'support-inline-placeholder'; }
   renderSupportInlineCustomerHistoryP19_(null);
@@ -729,7 +750,7 @@
 
   if (!supportCurrentRowNo) {
     document.getElementById('supportModalTitle').textContent = '영업지원 요청 추가';
-    document.getElementById('supportRequester').value = (supportCurrentData && supportCurrentData.currentUser) || '';
+    fillSupportRequesterSelect_('supportRequester', (supportCurrentData && supportCurrentData.currentUser) || '');
     document.getElementById('supportStatus').value = '접수';
     setTimeout(function(){ const el=document.getElementById('supportCustomerNo'); if(el) el.focus(); }, 80);
     return;
@@ -771,7 +792,10 @@
 function fillSupportSelects_(types, statuses) {
   const typeSel = document.getElementById('supportRequestType');
   const statusSel = document.getElementById('supportStatus');
+  const requesterSel = document.getElementById('supportRequester');
+  const prevRequester = requesterSel ? requesterSel.value : '';
   if (typeSel) typeSel.innerHTML = '<option value="">선택</option>' + (types || []).map(function(v){ return '<option value="' + escAttr(v) + '">' + esc(v) + '</option>'; }).join('');
+  fillSupportRequesterSelect_('supportRequester', prevRequester || (supportCurrentData && supportCurrentData.currentUser) || '');
   if (statusSel) {
     statusSel.innerHTML = (statuses || []).map(function(v){ return '<option value="' + escAttr(v) + '">' + esc(v) + '</option>'; }).join('');
     if (!canPortal_('canCompleteSupport')) {
@@ -785,7 +809,7 @@
   item = item || {};
   document.getElementById('supportReceiptNo').value = item.receiptNo || '';
   document.getElementById('supportRequestType').value = item.requestType || '';
-  document.getElementById('supportRequester').value = item.requester || '';
+  fillSupportRequesterSelect_('supportRequester', item.requester || '');
   document.getElementById('supportHandler').value = item.handler || '';
   document.getElementById('supportCustomerNo').value = item.customerNo || '';
   document.getElementById('supportCustomerName').value = item.customerName || '';
diff -ruN portal_step10_base_for_step11/Index.html portal_step11_work/Index.html
--- portal_step10_base_for_step11/Index.html	2026-06-27 03:32:07.000000000 +0000
+++ portal_step11_work/Index.html	2026-06-27 03:49:03.009437073 +0000
@@ -328,7 +328,7 @@
           </div>
           <div class="support-form-item">
             <div class="support-form-label">요청자</div>
-            <input id="supportRequester" placeholder="요청자명">
+            <select id="supportRequester"></select>
           </div>
           <div class="support-form-item">
             <div class="support-form-label">고객번호 / 고객명 검색 <span class="portal-guide-help" title="고객번호 또는 고객명을 입력한 뒤 불러오기/검색으로 기본 정보를 채웁니다.">?</span></div>
diff -ruN portal_step10_base_for_step11/STEP11_CHANGED_DIFF.patch portal_step11_work/STEP11_CHANGED_DIFF.patch
--- portal_step10_base_for_step11/STEP11_CHANGED_DIFF.patch	1970-01-01 00:00:00.000000000 +0000
+++ portal_step11_work/STEP11_CHANGED_DIFF.patch	2026-06-27 03:51:51.526126852 +0000
@@ -0,0 +1,403 @@
+diff -ruN portal_step10_base_for_step11/00_Config.gs portal_step11_work/00_Config.gs
+--- portal_step10_base_for_step11/00_Config.gs	2026-06-24 19:22:14.000000000 +0000
++++ portal_step11_work/00_Config.gs	2026-06-27 03:49:03.007740144 +0000
+@@ -162,7 +162,7 @@
+ 
+ const PORTAL_SUPPORT_REQUEST_TYPES = ['수기견적', '자료제출', '약속잡기', '확인하기', '기타요청'];
+ 
+-const PORTAL_SUPPORT_REQUESTER_OPTIONS = ['김경아', '최보람', '이옥희', '김서하', '방수원'];
++const PORTAL_SUPPORT_REQUESTER_OPTIONS = ['문형진', '방수원', '박새봄', '김경아', '최보람', '이옥희'];
+ 
+ const PORTAL_SUPPORT_STATUS_OPTIONS = ['접수', '처리중', '완료', '보류', '반려'];
+ 
+diff -ruN portal_step10_base_for_step11/09_SupportRequestService.gs portal_step11_work/09_SupportRequestService.gs
+--- portal_step10_base_for_step11/09_SupportRequestService.gs	2026-06-27 01:26:17.000000000 +0000
++++ portal_step11_work/09_SupportRequestService.gs	2026-06-27 03:51:15.771193508 +0000
+@@ -9,17 +9,49 @@
+ // 이 파일에서는 PORTAL_SUPPORT_REQUEST_TYPES / PORTAL_SUPPORT_REQUESTER_OPTIONS /
+ // PORTAL_SUPPORT_STATUS_OPTIONS / PORTAL_SUPPORT_MEMO_HEADER를 절대 재선언하지 않습니다.
+ 
+-function buildPortalSupportRequesterOptions_(currentUser) {
++function getPortalSupportDelegatedRequesterNamesP250_() {
++  return (PORTAL_SUPPORT_REQUESTER_OPTIONS || ['문형진', '방수원', '박새봄', '김경아', '최보람', '이옥희']).slice();
++}
++
++function canPortalChooseDelegatedSupportRequesterP250_(perm) {
++  perm = perm || getPortalCurrentPermission_();
++  return !!(perm && perm.active !== false && (perm.canUseAdminHome || perm.canCompleteSupport || perm.isAdmin));
++}
++
++function buildPortalSupportRequesterOptions_(currentUser, perm) {
++  perm = perm || getPortalCurrentPermission_();
++  currentUser = String(currentUser || getPortalCurrentUserName_() || '').trim();
+   const out = [];
+   function add(v) {
+     v = String(v || '').trim();
+     if (v && out.indexOf(v) < 0) out.push(v);
+   }
+-  add(currentUser || getPortalCurrentUserName_());
+-  (PORTAL_SUPPORT_REQUESTER_OPTIONS || []).forEach(add);
++
++  if (canPortalChooseDelegatedSupportRequesterP250_(perm)) {
++    getPortalSupportDelegatedRequesterNamesP250_().forEach(add);
++    add(currentUser);
++  } else {
++    add(currentUser);
++  }
+   return out;
+ }
+ 
++function assertPortalSupportRequesterAllowedP250_(requester, perm) {
++  perm = perm || getPortalCurrentPermission_();
++  requester = String(requester || '').trim();
++  const currentUser = getPortalCurrentUserName_();
++  const options = buildPortalSupportRequesterOptions_(currentUser, perm);
++  const normalizedRequester = normalizePortalNameForPermission_(requester);
++  const ok = options.some(function(v) {
++    return normalizePortalNameForPermission_(v) === normalizedRequester;
++  });
++  if (ok) return true;
++  if (canPortalChooseDelegatedSupportRequesterP250_(perm)) {
++    throw new Error('요청자는 지정된 요청자 목록에서만 선택할 수 있습니다.');
++  }
++  throw new Error('요청자는 본인만 선택할 수 있습니다.');
++}
++
+ 
+ // v64: 영업지원요청 목록 조회 속도 개선용 서버 캐시/읽기 전용 시트 접근
+ // - 목록 조회 때마다 헤더 보정/서식 쓰기를 하지 않습니다.
+@@ -121,7 +153,7 @@
+     requestTypes: PORTAL_SUPPORT_REQUEST_TYPES,
+     statusOptions: PORTAL_SUPPORT_STATUS_OPTIONS,
+     currentUser: getPortalCurrentUserName_(),
+-    requesterOptions: buildPortalSupportRequesterOptions_(getPortalCurrentUserName_()),
++    requesterOptions: buildPortalSupportRequesterOptions_(getPortalCurrentUserName_(), perm),
+     permission: sanitizePortalPermissionForClient_(perm),
+     meta: { mode: 'full', recent: false, complete: true, limit: limit, keyword: keyword, status: statusFilter, loadedAt: new Date().toISOString() }
+   };
+@@ -347,7 +379,7 @@
+     requestTypes: PORTAL_SUPPORT_REQUEST_TYPES,
+     statusOptions: PORTAL_SUPPORT_STATUS_OPTIONS,
+     currentUser: getPortalCurrentUserName_(),
+-    requesterOptions: buildPortalSupportRequesterOptions_(getPortalCurrentUserName_()),
++    requesterOptions: buildPortalSupportRequesterOptions_(getPortalCurrentUserName_(), perm),
+     permission: sanitizePortalPermissionForClient_(perm),
+     meta: meta
+   };
+@@ -356,7 +388,7 @@
+ // v78: 상세/처리 팝업 빠른 로딩용 상세 캐시 API
+ // - 목록 행에서 즉시 팝업을 채우고, 서버 상세는 ScriptCache를 먼저 봅니다.
+ // - 저장 시 bumpPortalSupportCacheBustV64_()가 호출되어 목록/상세 캐시가 같이 무효화됩니다.
+-const PORTAL_SUPPORT_DETAIL_CACHE_PREFIX_V78 = 'supportDetail:v78:';
++const PORTAL_SUPPORT_DETAIL_CACHE_PREFIX_V78 = 'supportDetail:v250:';
+ const PORTAL_SUPPORT_DETAIL_CACHE_TTL_SEC_V78 = 300;
+ 
+ function makePortalSupportDetailCacheKeyV78_(rowNo) {
+@@ -379,8 +411,12 @@
+       if (cached) {
+         const parsed = JSON.parse(cached);
+         if (parsed && parsed.item) {
++          const perm = getPortalCurrentPermission_();
+           parsed.fromCache = true;
+           parsed.loadedAt = parsed.loadedAt || new Date().toISOString();
++          parsed.currentUser = getPortalCurrentUserName_();
++          parsed.requesterOptions = buildPortalSupportRequesterOptions_(parsed.currentUser, perm);
++          parsed.permission = sanitizePortalPermissionForClient_(perm);
+           return parsed;
+         }
+       }
+@@ -393,13 +429,14 @@
+   const item = buildPortalSupportRowObject_(values, rowNo, headerMap);
+   if (!item || (!item.requestText && !item.customerNo && !item.customerName)) throw new Error('영업지원 요청을 찾지 못했습니다.');
+ 
++  const perm = getPortalCurrentPermission_();
+   const result = {
+     item: item,
+     requestTypes: PORTAL_SUPPORT_REQUEST_TYPES,
+     statusOptions: PORTAL_SUPPORT_STATUS_OPTIONS,
+     currentUser: getPortalCurrentUserName_(),
+-    requesterOptions: buildPortalSupportRequesterOptions_(getPortalCurrentUserName_()),
+-    permission: sanitizePortalPermissionForClient_(getPortalCurrentPermission_()),
++    requesterOptions: buildPortalSupportRequesterOptions_(getPortalCurrentUserName_(), perm),
++    permission: sanitizePortalPermissionForClient_(perm),
+     fromCache: false,
+     loadedAt: new Date().toISOString()
+   };
+@@ -592,6 +629,7 @@
+ 
+   if (!requestType) throw new Error('업무유형을 선택하세요.');
+   if (!requester) throw new Error('요청자를 입력하세요.');
++  assertPortalSupportRequesterAllowedP250_(requester, permForSupport);
+   if (!requestText) throw new Error('요청업무를 입력하세요.');
+ 
+   const receiptNo = String(payload.receiptNo || '').trim() || getExistingOrNextPortalSupportReceiptNo_(sheet, headerMap, rowNo);
+diff -ruN portal_step10_base_for_step11/15_PermissionService.gs portal_step11_work/15_PermissionService.gs
+--- portal_step10_base_for_step11/15_PermissionService.gs	2026-06-27 02:51:37.000000000 +0000
++++ portal_step11_work/15_PermissionService.gs	2026-06-27 03:50:19.170230309 +0000
+@@ -44,7 +44,7 @@
+ 
+ // STEP6 P230: 권한_DB 반복 조회를 줄이기 위한 짧은 서버 캐시입니다.
+ // 권한 정책은 그대로 두고, 같은 사용자 이메일의 계산 완료 권한 객체만 5분 이내 재사용합니다.
+-const PORTAL_PERMISSION_CACHE_PREFIX_P230 = 'PORTAL_PERMISSION_P230_';
++const PORTAL_PERMISSION_CACHE_PREFIX_P230 = 'PORTAL_PERMISSION_P250_';
+ const PORTAL_PERMISSION_CACHE_BUST_PROP_P230 = 'PORTAL_PERMISSION_CACHE_BUST_P230';
+ const PORTAL_PERMISSION_CACHE_TTL_SEC_P230 = 300;
+ const PORTAL_PERMISSION_GUEST_CACHE_TTL_SEC_P230 = 60;
+@@ -324,11 +324,14 @@
+   // - 영업지원완료처리: 영업지원 처리/완료 입력 권한
+   // - 작업로그전체열람/작업로그비교통계: 작업로그 전용 권한
+   // SALES 계정에 전체고객열람=Y가 있어도 HOME/작업로그/완료처리는 관리자 권한으로 보지 않습니다.
++  const active = String(v(14) || 'Y').toUpperCase() !== 'N';
+   const baseAdmin = isPortalAdminPermissionValues_(level, role, rank, defaultScope);
+   const canViewAllCustomers = baseAdmin || yn_(v(8)) || defaultScope === 'ALL';
+   const canWriteNotice = baseAdmin || yn_(v(9));
+   const canDeleteNotice = baseAdmin || yn_(v(10));
+-  const canWriteSupport = baseAdmin || yn_(v(11));
++  // STEP11 P250: 영업지원 요청 작성은 모든 등록/활성 사용자에게 허용합니다.
++  // 완료 처리/전체 열람 권한은 기존 권한_DB 정책을 그대로 유지합니다.
++  const canWriteSupport = active;
+   const canReadAllSupport = baseAdmin || yn_(v(12));
+   const canCompleteSupport = baseAdmin || yn_(v(13));
+   const canViewAllActivityLogs = baseAdmin || yn_(v(16));
+@@ -355,7 +358,7 @@
+     canWriteSupport: canWriteSupport,
+     canReadAllSupport: canReadAllSupport,
+     canCompleteSupport: canCompleteSupport,
+-    active: String(v(14) || 'Y').toUpperCase() !== 'N',
++    active: active,
+     note: v(15),
+     canViewAllActivityLogs: canViewAllActivityLogs,
+     canCompareActivityLogs: canCompareActivityLogs
+@@ -610,7 +613,7 @@
+ 
+ function assertPortalCanWriteSupport_() {
+   const perm = getPortalCurrentPermission_();
+-  if (perm.canWriteSupport) return true;
++  if (perm && perm.active !== false && perm.canWriteSupport) return true;
+   throw new Error('영업지원 요청 작성 권한이 없습니다.');
+ }
+ 
+diff -ruN portal_step10_base_for_step11/Component_Send.html portal_step11_work/Component_Send.html
+--- portal_step10_base_for_step11/Component_Send.html	2026-06-27 01:38:42.000000000 +0000
++++ portal_step11_work/Component_Send.html	2026-06-27 03:49:03.010867042 +0000
+@@ -92,6 +92,7 @@
+         '<button type="button" class="send-file-preset-btn" onclick="applySendFileQuickPreset_(&quot;first&quot;)">#최초</button>' +
+         '<button type="button" class="send-file-preset-btn" onclick="applySendFileQuickPreset_(&quot;required&quot;)">#필수</button>' +
+         '<button type="button" class="send-file-preset-btn" onclick="applySendFileQuickPreset_(&quot;contractor&quot;)">#수행사</button>' +
++        '<button type="button" class="send-file-preset-btn send-file-clear-btn" onclick="clearAllSendFileChecksP250_()">모든 체크 해제</button>' +
+       '</div>' +
+     '</div>';
+ }
+@@ -126,6 +127,18 @@
+   renderSendPreview();
+ }
+ 
++function clearAllSendFileChecksP250_() {
++  document.querySelectorAll('.send-file-check').forEach(function(el) {
++    el.checked = false;
++  });
++  sendSelectedFileKeysV50 = [];
++  clearSendReviewSessionV75_('all file checks cleared');
++  captureSendGlobalsToWorkspaceP211_(getActiveSendWorkspaceP211_());
++  renderSendPreview();
++  renderSendWorkspaceTabsP211_();
++  toast('발송자료 체크를 모두 해제했습니다.');
++}
++
+ // PATCH N: 견적/자료 발송 고객검색은 화면 부담을 줄이기 위해 5건 단위로 표시합니다.
+ // - 고객 상세 검색 화면의 20건 페이지와 분리하여, 발송 화면 전용 페이지 크기를 사용합니다.
+ // - 검색인덱스_DB가 준비되어 있으면 브라우저 인덱스에서 즉시 5건 단위로 표시하고,
+@@ -654,6 +667,7 @@
+       const base = buildSendWorkspaceP211_(idx + 1);
+       const merged = Object.assign(base, ws || {});
+       merged.order = idx + 1;
++      merged.createdAt = Number(merged.createdAt || (savedAt ? savedAt + idx : Date.now() + idx));
+       merged.mailProgressPollTimer = null;
+       merged.recipientTouched = Object.assign({ to:false, cc:false }, merged.recipientTouched || {});
+       merged.searchState = Object.assign({ keyword:'', page:0, pageSize:SEND_CUSTOMER_PAGE_SIZE }, merged.searchState || {});
+@@ -685,6 +699,7 @@
+   return {
+     id: 'send_ws_' + (Date.now()) + '_' + (sendWorkspaceSeedP211_++),
+     order: Number(order || 1),
++    createdAt: Date.now(),
+     title: '발송고객 ' + Number(order || 1),
+     customerName: '',
+     searchState: { keyword: '', page: 0, pageSize: SEND_CUSTOMER_PAGE_SIZE },
+@@ -1196,14 +1211,15 @@
+   });
+ }
+ 
+-function selectSendCustomer(rowNo) {
++function selectSendCustomer(rowNo, options) {
++  options = options || {};
+   const ws = getActiveSendWorkspaceP211_();
+   if (!ws) return;
+   applySendWorkspaceToGlobalsP211_(ws);
+   rowNo = Number(rowNo) || 0;
+   const summary = findCustomerRowSummaryByRowNo_(rowNo) || {};
+   const targetCustomerNo = normalizeCustomerNoForKey_(summary.customerNo || (sendSelectedDetail && sendSelectedDetail.customerNo) || sendSelectedCustomerNoV50);
+-  const dupWs = findSendWorkspaceByCustomerNoP212_(targetCustomerNo, ws.id);
++  const dupWs = options.skipDuplicateCheck ? null : findSendWorkspaceByCustomerNoP212_(targetCustomerNo, ws.id);
+   if (dupWs) {
+     toast('이미 다른 발송 작업창에서 선택한 고객입니다. 해당 탭으로 이동합니다.');
+     activateSendWorkspaceTabP211_(dupWs.id);
+@@ -1651,11 +1667,58 @@
+   });
+ }
+ 
++function resetSendWorkspaceForExternalOpenP250_(ws) {
++  if (!ws) return null;
++  const oldOrder = Number(ws.order || 1) || 1;
++  try { if (ws.mailProgressPollTimer) clearTimeout(ws.mailProgressPollTimer); } catch (err) {}
++  const next = buildSendWorkspaceP211_(oldOrder);
++  Object.keys(ws).forEach(function(k) { delete ws[k]; });
++  Object.assign(ws, next, { order: oldOrder, title: '발송고객 ' + oldOrder, customerName: '' });
++  return ws;
++}
++
++function getOldestSendWorkspaceForReuseP250_() {
++  ensureSendWorkspacesP211_();
++  if (!sendWorkspacesP211_.length) return null;
++  return sendWorkspacesP211_.slice().sort(function(a, b) {
++    const at = Number(a && a.createdAt || 0) || Number(a && a.order || 0) || 0;
++    const bt = Number(b && b.createdAt || 0) || Number(b && b.order || 0) || 0;
++    if (at !== bt) return at - bt;
++    return (Number(a && a.order || 0) || 0) - (Number(b && b.order || 0) || 0);
++  })[0] || null;
++}
++
++function createSendWorkspaceForCustomerOpenP250_() {
++  ensureSendWorkspacesP211_();
++  const current = getActiveSendWorkspaceP211_();
++  if (current) captureSendGlobalsToWorkspaceP211_(current);
++
++  let ws;
++  if (sendWorkspacesP211_.length < SEND_WORKSPACE_MAX_P211_) {
++    ws = buildSendWorkspaceP211_(sendWorkspacesP211_.length + 1);
++    sendWorkspacesP211_.push(ws);
++  } else {
++    ws = resetSendWorkspaceForExternalOpenP250_(getOldestSendWorkspaceForReuseP250_());
++    toast('발송 작업창이 5개라 가장 오래된 작업창을 새 고객으로 교체합니다.');
++  }
++
++  sendWorkspacesP211_.forEach(function(item, i) {
++    item.order = i + 1;
++    if (!item.customerName) item.title = '발송고객 ' + (i + 1);
++  });
++  sendActiveWorkspaceIdP211_ = ws ? ws.id : '';
++  persistSendWorkspacesP213_(true);
++  return ws;
++}
++
+ function openSendForCustomer(rowNo) {
+   switchPage('send', true);
+   setTimeout(function(){
+-    if (!getActiveSendWorkspaceP211_()) ensureSendWorkspacesP211_();
+-    selectSendCustomer(Number(rowNo));
++    const ws = createSendWorkspaceForCustomerOpenP250_();
++    if (!ws) return;
++    renderSendWorkspaceTabsP211_();
++    renderSendWorkspaceBodyP211_();
++    selectSendCustomer(Number(rowNo), { skipDuplicateCheck: true });
+   }, 120);
+ }
+ 
+diff -ruN portal_step10_base_for_step11/Component_Support.html portal_step11_work/Component_Support.html
+--- portal_step10_base_for_step11/Component_Support.html	2026-06-27 01:38:42.000000000 +0000
++++ portal_step11_work/Component_Support.html	2026-06-27 03:49:03.009932055 +0000
+@@ -345,27 +345,48 @@
+ }
+ 
+ function getSupportRequesterOptions_() {
+-  const base = ['김경아','최보람','이옥희','김서하','방수원'];
+   const current = (supportCurrentData && supportCurrentData.currentUser) || '';
+   const fromServer = (supportCurrentData && supportCurrentData.requesterOptions) || [];
+   const out = [];
+   function add(v) { v = String(v || '').trim(); if (v && out.indexOf(v) < 0) out.push(v); }
+-  add(current);
+   fromServer.forEach(add);
+-  base.forEach(add);
++  add(current);
+   return out;
+ }
+ 
++function renderSupportRequesterOptionsHtml_(selected) {
++  const options = getSupportRequesterOptions_();
++  selected = String(selected || '').trim();
++  if (selected && options.indexOf(selected) < 0) options.unshift(selected);
++  return options.map(function(v) {
++    const sel = String(v || '') === selected ? ' selected' : '';
++    return '<option value="' + escAttr(v) + '"' + sel + '>' + esc(v) + '</option>';
++  }).join('');
++}
++
++function fillSupportRequesterSelect_(id, selected) {
++  const el = document.getElementById(id);
++  if (!el) return;
++  const current = String(selected || (supportCurrentData && supportCurrentData.currentUser) || '').trim();
++  if (el.tagName && String(el.tagName).toUpperCase() === 'SELECT') {
++    el.innerHTML = renderSupportRequesterOptionsHtml_(current);
++    if (current) el.value = current;
++  } else {
++    el.value = current;
++    const options = getSupportRequesterOptions_();
++    if (options.length <= 1) el.readOnly = true;
++  }
++}
++
+ function initSupportInlineEntryDefaults_() {
+-  const requester = document.getElementById('supportInlineRequester');
+-  if (requester && supportCurrentData && supportCurrentData.currentUser) requester.value = supportCurrentData.currentUser;
++  fillSupportRequesterSelect_('supportInlineRequester', (supportCurrentData && supportCurrentData.currentUser) || '');
+   window.__supportInlineCustomer = null;
+ }
+ 
+ function resetSupportInlineEntry() {
+   ['supportInlineCustomerNo','supportInlineCustomerName','supportInlineRequestText'].forEach(function(id){ const el=document.getElementById(id); if(el) el.value=''; });
+   const type = document.getElementById('supportInlineRequestType'); if (type) type.value = '';
+-  const requester = document.getElementById('supportInlineRequester'); if (requester && supportCurrentData && supportCurrentData.currentUser) requester.value = supportCurrentData.currentUser;
++  fillSupportRequesterSelect_('supportInlineRequester', (supportCurrentData && supportCurrentData.currentUser) || '');
+   const summary = document.getElementById('supportInlineContractSummary');
+   if (summary) { summary.textContent = '고객번호 입력 후 적용을 누르면 계약조건과 고객 정보가 자동으로 표시됩니다.'; summary.className = 'support-inline-placeholder'; }
+   renderSupportInlineCustomerHistoryP19_(null);
+@@ -729,7 +750,7 @@
+ 
+   if (!supportCurrentRowNo) {
+     document.getElementById('supportModalTitle').textContent = '영업지원 요청 추가';
+-    document.getElementById('supportRequester').value = (supportCurrentData && supportCurrentData.currentUser) || '';
++    fillSupportRequesterSelect_('supportRequester', (supportCurrentData && supportCurrentData.currentUser) || '');
+     document.getElementById('supportStatus').value = '접수';
+     setTimeout(function(){ const el=document.getElementById('supportCustomerNo'); if(el) el.focus(); }, 80);
+     return;
+@@ -771,7 +792,10 @@
+ function fillSupportSelects_(types, statuses) {
+   const typeSel = document.getElementById('supportRequestType');
+   const statusSel = document.getElementById('supportStatus');
++  const requesterSel = document.getElementById('supportRequester');
++  const prevRequester = requesterSel ? requesterSel.value : '';
+   if (typeSel) typeSel.innerHTML = '<option value="">선택</option>' + (types || []).map(function(v){ return '<option value="' + escAttr(v) + '">' + esc(v) + '</option>'; }).join('');
++  fillSupportRequesterSelect_('supportRequester', prevRequester || (supportCurrentData && supportCurrentData.currentUser) || '');
+   if (statusSel) {
+     statusSel.innerHTML = (statuses || []).map(function(v){ return '<option value="' + escAttr(v) + '">' + esc(v) + '</option>'; }).join('');
+     if (!canPortal_('canCompleteSupport')) {
+@@ -785,7 +809,7 @@
+   item = item || {};
+   document.getElementById('supportReceiptNo').value = item.receiptNo || '';
+   document.getElementById('supportRequestType').value = item.requestType || '';
+-  document.getElementById('supportRequester').value = item.requester || '';
++  fillSupportRequesterSelect_('supportRequester', item.requester || '');
+   document.getElementById('supportHandler').value = item.handler || '';
+   document.getElementById('supportCustomerNo').value = item.customerNo || '';
+   document.getElementById('supportCustomerName').value = item.customerName || '';
+diff -ruN portal_step10_base_for_step11/Index.html portal_step11_work/Index.html
+--- portal_step10_base_for_step11/Index.html	2026-06-27 03:32:07.000000000 +0000
++++ portal_step11_work/Index.html	2026-06-27 03:49:03.009437073 +0000
+@@ -328,7 +328,7 @@
+           </div>
+           <div class="support-form-item">
+             <div class="support-form-label">요청자</div>
+-            <input id="supportRequester" placeholder="요청자명">
++            <select id="supportRequester"></select>
+           </div>
+           <div class="support-form-item">
+             <div class="support-form-label">고객번호 / 고객명 검색 <span class="portal-guide-help" title="고객번호 또는 고객명을 입력한 뒤 불러오기/검색으로 기본 정보를 채웁니다.">?</span></div>
diff -ruN portal_step10_base_for_step11/STEP11_REQUESTED_FIXES_REPORT.txt portal_step11_work/STEP11_REQUESTED_FIXES_REPORT.txt
--- portal_step10_base_for_step11/STEP11_REQUESTED_FIXES_REPORT.txt	1970-01-01 00:00:00.000000000 +0000
+++ portal_step11_work/STEP11_REQUESTED_FIXES_REPORT.txt	2026-06-27 03:51:51.382334868 +0000
@@ -0,0 +1,45 @@
+STEP11_REQUESTED_FIXES_REPORT
+기준선: STEP10_동시성테스트패널추가
+목적: 동시성 테스트 전 사용자 요청 4건 반영
+
+수정 파일:
+- 00_Config.gs
+- 09_SupportRequestService.gs
+- 15_PermissionService.gs
+- Index.html
+- Component_Support.html
+- Component_Send.html
+- Styles.html
+
+1) 영업지원 요청 작성 권한/요청자 선택 규칙
+- 영업지원 요청 작성 권한은 권한_DB에 등록된 활성 사용자 전체로 확대했습니다.
+- 단, 완료 처리/전체 열람/관리자 홈 권한은 기존 정책을 유지했습니다.
+- 서무/책임/관리자성 계정(canUseAdminHome 또는 canCompleteSupport)은 요청자를 아래 목록에서 선택할 수 있습니다.
+  문형진 / 방수원 / 박새봄 / 김경아 / 최보람 / 이옥희
+- 일반 사용자는 요청자 옵션이 현재 사용자 본인 1명만 표시됩니다.
+- 프론트 select 제한만으로 끝내지 않고 savePortalSupportRequest 저장 시 assertPortalSupportRequesterAllowedP250_()로 서버 검증도 추가했습니다.
+- 기존 support detail ScriptCache가 다른 사용자 requesterOptions를 반환하지 않도록 캐시 hit 시 permission/requesterOptions/currentUser를 현재 사용자 기준으로 재계산합니다.
+- 권한 캐시 prefix를 P250으로 변경해 기존 5분 캐시가 canWriteSupport 변경을 붙잡지 않게 했습니다.
+
+2) 고객 상세검색 -> 자료발송 이동 시 새 작업창 추가
+- openSendForCustomer(rowNo)를 변경했습니다.
+- 고객 상세검색의 자료발송 버튼으로 넘어올 때 기존 활성 발송 작업창을 덮어쓰지 않고 새 발송 작업창을 생성합니다.
+- 이미 5개가 열려 있으면 createdAt/order 기준 가장 오래된 작업창을 초기화해 새 고객으로 교체합니다.
+- 발송 화면 내부에서 고객을 검색해 선택할 때의 기존 중복 고객 방지 로직은 유지했습니다.
+- 외부 이동(openSendForCustomer)에서만 selectSendCustomer(rowNo, { skipDuplicateCheck: true })를 사용합니다.
+
+3) 자료발송 하단 버튼 데크 축소
+- .send-fixed-footer를 전체 폭 sticky bar가 아니라 버튼 묶음 크기에 맞는 width:max-content 캡슐형 영역으로 변경했습니다.
+- 기존 버튼 3개와 기능은 유지했습니다.
+- .send-workspace padding-bottom도 과도하지 않게 줄였습니다.
+
+4) 자료발송 모든 체크 해제 버튼 추가
+- 발송자료 빠른 선택 영역에 [모든 체크 해제] 버튼을 추가했습니다.
+- clearAllSendFileChecksP250_()에서 체크박스를 모두 해제하고 selectedFileKeys/reviewSession/preview/workspace 상태를 같이 갱신합니다.
+
+회귀 방지 확인:
+- 모든 .gs 문법 검사 통과
+- 모든 .html script 문법 검사 통과
+- 수정 파일 내부 top-level function 중복 없음
+- 주요 기존 함수 존재 확인 완료
+- 파일 삭제 없음
diff -ruN portal_step10_base_for_step11/STEP11_STATIC_CHECK.json portal_step11_work/STEP11_STATIC_CHECK.json
--- portal_step10_base_for_step11/STEP11_STATIC_CHECK.json	1970-01-01 00:00:00.000000000 +0000
+++ portal_step11_work/STEP11_STATIC_CHECK.json	2026-06-27 03:51:51.382165985 +0000
@@ -0,0 +1,243 @@
+{
+  "step": "STEP11",
+  "base": "STEP10_동시성테스트패널추가",
+  "modifiedFiles": [
+    "00_Config.gs",
+    "09_SupportRequestService.gs",
+    "15_PermissionService.gs",
+    "Component_Send.html",
+    "Component_Support.html",
+    "Index.html",
+    "Styles.html"
+  ],
+  "syntaxOk": true,
+  "syntaxChecks": [
+    {
+      "file": "00_Config.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "01_Schema.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "02_Utils.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "03_DbAccess.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "04_MasterRepository.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "05_CustomerService.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "06_ContactHistoryService.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "07_NoticeService.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "08_TodayService.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "09_SupportRequestService.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "10_DashboardService.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "11_MailBridgeService.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "12_SetupDiagnostics.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "13_SettingsService.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "14_FavoriteService.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "15_PermissionService.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "16_ContractCompleteService.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "17_ActivityLogService.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "18_ConcurrencySyncService.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "19_RequestGuardService.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "20_SystemHealthService.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "21_ChangeQueueService.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "22_ConcurrencyTestService.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "Code.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "PortalMailWorkerFallbackConfig.gs",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "Client_Router.html#script0",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "Client_State.html#script0",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "Client_Utils.html#script0",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "Component_ActivityLog.html#script0",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "Component_ConcurrencyTest.html#script0",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "Component_ContractComplete.html#script0",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "Component_CustomerDetail.html#script0",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "Component_CustomerSearch.html#script0",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "Component_Favorites.html#script0",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "Component_Home.html#script0",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "Component_Modals.html#script0",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "Component_Notice.html#script0",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "Component_Send.html#script0",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "Component_Support.html#script0",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "Component_SystemHealth.html#script0",
+      "ok": true,
+      "error": ""
+    },
+    {
+      "file": "Component_Today.html#script0",
+      "ok": true,
+      "error": ""
+    }
+  ],
+  "internalFunctionDuplicates": {
+    "00_Config.gs": [],
+    "09_SupportRequestService.gs": [],
+    "15_PermissionService.gs": [],
+    "Component_Send.html": [],
+    "Component_Support.html": [],
+    "Index.html": [],
+    "Styles.html": []
+  },
+  "keyFunctions": {
+    "savePortalSupportRequest": true,
+    "assertPortalCanWriteSupport_": true,
+    "buildPortalSupportRequesterOptions_": true,
+    "assertPortalSupportRequesterAllowedP250_": true,
+    "openSendForCustomer": true,
+    "selectSendCustomer": true,
+    "createSendWorkspaceForCustomerOpenP250_": true,
+    "clearAllSendFileChecksP250_": true,
+    "sendPortalMailFromUi": true,
+    "preparePortalMailFilesForReviewFromUi": true
+  },
+  "createdAt": "2026-06-27T03:51:51.381922"
+}
\ No newline at end of file
diff -ruN portal_step10_base_for_step11/Styles.html portal_step11_work/Styles.html
--- portal_step10_base_for_step11/Styles.html	2026-06-27 01:38:42.000000000 +0000
+++ portal_step11_work/Styles.html	2026-06-27 03:49:03.012141352 +0000
@@ -122,7 +122,7 @@
     .send-chip-muted { color:#667085; font-weight:700; }
     .send-preview-warn { margin-top:12px; border:1px solid #fed7aa; background:#fffaf5; color:#9a3412; border-radius:10px; padding:9px 11px; font-size:12px; line-height:1.55; }
     .send-warning { border:1px solid #fed7aa; background:#fffaf5; color:#9a3412; padding:10px 12px; border-radius:12px; font-size:13px; line-height:1.6; margin-top:10px; }
-    .send-workspace { display:flex; flex-direction:column; gap:14px; padding-bottom:76px; }
+    .send-workspace { display:flex; flex-direction:column; gap:14px; padding-bottom:24px; }
     .send-top-search { display:grid; grid-template-columns: 220px minmax(260px,1fr) 108px 108px; gap:18px; align-items:center; }
     .send-step-title { font-size:21px; font-weight:900; letter-spacing:-0.04em; color:#101828; white-space:nowrap; }
     .send-big-box { border:1px solid #e4e7ec; border-radius:16px; background:#fff; padding:18px 20px; box-shadow:0 8px 20px rgba(16,24,40,.035); }
@@ -136,8 +136,8 @@
     .send-selected-val { color:#202124; font-weight:700; text-align:center; word-break:break-word; }
     .send-two-col { display:grid; grid-template-columns: 1fr 1fr; gap:18px; align-items:stretch; }
     .send-tall-card { min-height:300px; }
-    .send-fixed-footer { position:sticky; bottom:0; z-index:20; background:rgba(255,255,255,.96); border-top:1px solid #e4e7ec; padding:14px 18px; display:flex; justify-content:flex-end; gap:14px; box-shadow:0 -8px 20px rgba(16,24,40,.08); margin:0 -18px -18px -18px; }
-    .send-fixed-footer button { min-width:180px; min-height:46px; font-size:15px; }
+    .send-fixed-footer { position:sticky; bottom:10px; z-index:20; width:max-content; max-width:100%; margin:12px 0 0 auto; background:rgba(255,255,255,.96); border:1px solid #e4e7ec; border-radius:999px; padding:8px; display:flex; justify-content:flex-end; gap:8px; box-shadow:0 8px 24px rgba(16,24,40,.12); }
+    .send-fixed-footer button { min-width:172px; min-height:42px; font-size:14px; }
     .recipient-line { display:grid; grid-template-columns:70px 1fr 52px; gap:8px; align-items:start; margin-bottom:10px; }
     .recipient-line-label { color:#667085; font-weight:900; padding-top:4px; }
     .send-chip.editable { padding-right:7px; }
@@ -1984,7 +1984,7 @@
 .send-file-preset-actions {
   flex: 1;
   display: grid;
-  grid-template-columns: repeat(4, minmax(120px, 1fr));
+  grid-template-columns: repeat(5, minmax(110px, 1fr));
   gap: 12px 16px;
   align-items: center;
 }
@@ -2008,6 +2008,11 @@
   transform: translateY(-1px);
 }
 
+.send-file-clear-btn {
+  color: #475467;
+  background: rgba(255,255,255,.82);
+}
+
 @media (max-width: 1280px) {
   .send-workspace-tabs-row {
     flex-direction: column;
@@ -4117,7 +4122,7 @@
 }
 .system-health-summary-p208 {
   display: grid;
-  grid-template-columns: repeat(4, minmax(120px, 1fr));
+  grid-template-columns: repeat(5, minmax(110px, 1fr));
   gap: 12px;
   padding: 18px 24px 6px;
 }
